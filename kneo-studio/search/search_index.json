{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Kneo Agent Studio","text":"<p>The development and migration UI for the Kneo Agent Platform.</p> <p>Studio turns prototype agents built in n8n or Dify into governed, observable, auditable Kneo specs \u2014 and tells you, in detail, what it could not carry across. Every construct Kneo has no form for (output parsers, memory, retrieval, inline secrets, branch predicates, prompt variables) becomes a gap card you have to see; blocking cards refuse export until they are resolved.</p> <p>The split with Kneo Agent Dashboard is by environment: Studio proves a spec compiles, test-runs, and is debuggable in dev / local, then hands it off; the Dashboard's Launch takes it into staging / production and operates it.</p> <p>Studio creates governed Kneo specs. Dashboard launches and operates them.</p>"},{"location":"#architecture","title":"Architecture","text":"<pre><code>React/TS SPA  \u2500\u2500/api/*\u2500\u2500\u25b6  FastAPI BFF  \u2500\u2500\u252c\u2500\u2500\u25b6 kneo_client.agent \u2500\u2500/v1\u2500\u2500\u25b6 kneo-serv\n (frontend)                (backend)      \u2502     (validate/compile/graph/policy)\n                                          \u251c\u2500\u2500\u25b6 kneo_client.platform  (read-only pre-flight)\n                                          \u2514\u2500\u2500\u25b6 importers / normalize / gap  (studio-domain, local)\n</code></pre> <ul> <li>Backend \u2014 a thin FastAPI BFF. Platform routes go through <code>kneo_client</code>;   the studio-domain importers run locally: each source's adapter reads its own   format into a neutral IR, and one lowering turns that IR into a Kneo   <code>version: v1</code> spec. <code>kneo_client</code> is its only Kneo dependency.</li> <li>Frontend \u2014 a React + TypeScript SPA. YAML is the source of truth; the   graph canvas is a read-only projection of the topology the platform returns.</li> </ul>"},{"location":"#the-pipeline","title":"The pipeline","text":"<pre><code>Import artifact \u2192 map nodes/tools/secrets \u2192 generate Kneo spec \u2192 show gaps\n  \u2192 add policy/HITL \u2192 validate/compile \u2192 test run + debug (dev/local) \u2192 hand off\n                            \u2191___________________________|\n                              \"why did it do that?\"\n</code></pre> <p>Import \u2014 n8n workflows and Dify app exports. Unsupported constructs become \"manual mapping required\" cards; a workflow that cannot be mapped onto a Kneo family at all is reported as such rather than wrapped.</p> <p>Debug \u2014 the run's graph animated by its live position, the trace, checkpoints and their diff, and Stop. Replay and resume are later cuts.</p> <p>Hand off \u2014 export the governed spec as YAML, or convert it on the Hand-off page into the inline JSON the Dashboard's Launch accepts.</p> <p>Deployment security</p> <p>Studio is a multi-user server whose default deployment is your own machine. With the default <code>auth_mode = none</code> it binds loopback, needs no login, and refuses to start on a routable address. To share an instance with a team, set <code>KNEO_STUDIO_AUTH_MODE</code> to <code>static</code> or <code>oidc</code> \u2014 see hosting for a team.</p>"},{"location":"#run","title":"Run","text":"<p>The published wheel bundles the built SPA, so a bare install serves the full UI and API same-origin \u2014 no container and no database service (SQLite is created for you):</p> <pre><code>pip install kneo-studio\nkneo-studio        # serves UI + /api on 127.0.0.1:8091\n</code></pre> <p>A container image (<code>ghcr.io/kneo-agent/kneo-studio</code>) is also published for shared hosts, with PostgreSQL support built in.</p> <p>Start with the quickstart. The examples are four n8n workflows and one Dify app, with the measured outcome of importing each \u2014 import <code>chat-agent.json</code> first, the clean reference.</p>"},{"location":"examples/","title":"Example imports","text":"<p>Four n8n workflows and one Dify app to import into Studio, and \u2014 more usefully \u2014 what each one actually produces. Every figure below was measured by running the file through the importer, not estimated. The Dify example needs 0.9.0 or later: 0.8.0 and earlier answer a Dify import with <code>501 not_implemented</code>.</p> <p>Import one from the SPA, or over the API:</p> <pre><code>curl -X POST http://127.0.0.1:8091/api/projects/default/import/n8n \\\n  -H 'Content-Type: application/json' \\\n  -d \"$(jq -n --rawfile raw examples/n8n/chat-agent.json '{raw: $raw}')\"\n</code></pre> <p>On a project that is already bound to a platform environment, that answers <code>422 alias_required</code>. A bound project has an approved generation, so an import has to say which alias it belongs to \u2014 add <code>alias</code> beside <code>raw</code>:</p> <pre><code>  -d \"$(jq -n --rawfile raw examples/n8n/chat-agent.json \\\n        '{raw: $raw, alias: \"default\"}')\"\n</code></pre> <p>The alias must name one of the project's environments \u2014 <code>default</code> is the one the zero-config binding creates; any other name answers <code>422 unknown_alias</code>. The command above is written for a fresh instance, where <code>default</code> is unbound. The two are exclusive in the other direction too: an alias on an unbound project is <code>409 project_unbound</code>, because there is no approved generation for it to name.</p> File Blocking gaps Manual gaps Info notes Converts to <code>n8n/chat-agent.json</code> none <code>entry_mechanism_not_carried</code> (the trigger) the iteration bound a one-agent spec <code>n8n/hello-agent.json</code> 1 (<code>source_cannot_run</code>) <code>entry_mechanism_not_carried</code> (the trigger) the iteration bound a one-agent spec n8n itself never ran <code>n8n/agent-with-tool.json</code> 2 (<code>source_cannot_run</code>, <code>tool_without_backing</code>) <code>entry_mechanism_not_carried</code> (the trigger) the iteration bound a one-agent spec, with the tool left out and carded <code>n8n/needs-work.json</code> 3 <code>entry_mechanism_not_carried</code> (the trigger) \u2014 nothing usable \u2014 every step is unrepresentable"},{"location":"examples/#the-one-thing-to-know-first","title":"The one thing to know first","text":"<p>Every n8n workflow's trigger becomes a card, and that is normal. Kneo has no trigger concept, so the card says \"Trigger must be recreated outside Kneo\". For a webhook, chat, schedule or event trigger it is <code>manual</code>, because the route or schedule is real behaviour to recreate; for a manual trigger it is <code>info</code>. A first import showing that one manual gap, and otherwise only <code>info</code> notes, is the clean outcome. An <code>info</code> note asks nothing of you: here it says the agent's tool loop runs with n8n's default iteration bound, which the spec leaves to the platform's default. <code>chat-agent.json</code> exists so you have something to compare against, because \"one gap\" is otherwise indistinguishable from \"something went wrong\".</p>"},{"location":"examples/#why-hello-agentjson-blocks","title":"Why <code>hello-agent.json</code> blocks","text":"<p>It is a workflow n8n itself refuses to run, and Studio says so rather than handing you a working agent the source never was. A schedule starts it, and the agent's prompt is <code>auto</code>, which reads the incoming item's <code>chatInput</code>, but a schedule's item has no <code>chatInput</code>. On top of that, an OpenAI model under n8n's tools agent needs at least one tool, and it has none. The pinned n8n fails that agent on every item, so the card is <code>source_cannot_run</code>, and the fix belongs in the source. <code>chat-agent.json</code> is the same agent made runnable. A chat trigger supplies <code>chatInput</code>, and an Anthropic model needs no tool. <code>agent-with-tool.json</code> has the first defect too, because a webhook's item carries the request under <code>body</code>, not <code>chatInput</code>.</p>"},{"location":"examples/#what-converts","title":"What converts","text":"<p>Measured against the pinned release (<code>n8n@1.62.1</code> \u2014 see <code>N8N_COMPAT</code>):</p> <p>Converts. Agent nodes and the language model attached to one, where n8n itself can run the agent: an agent n8n fails on every item \u2014 no model, a model its executor refuses, or an <code>auto</code> prompt with no <code>chatInput</code> to read \u2014 is a blocking <code>source_cannot_run</code> card instead. Several agents in sequence become a <code>sequential</code> workflow \u2014 each agent after the first needs a defined prompt, since an <code>auto</code> prompt after an agent has no <code>chatInput</code> to read. An <code>if</code> between agents becomes a <code>graph</code> workflow, so the branching shape survives.</p> <p>Tools do not convert, and this is the limitation to know about. A Kneo tool is backed by exactly one of a Python <code>implementation</code> import path, a declared MCP server, or another agent \u2014 and an n8n export describes none of them. A <code>toolHttpRequest</code> names an ordinary REST endpoint, which is not an MCP server: pointing Kneo at it would send MCP protocol traffic to your API. A <code>toolCode</code> is JavaScript, where an <code>implementation</code> is a Python import path.</p> <p>So every <code>ai_tool</code> attachment that is a tool node (<code>toolHttpRequest</code>, <code>toolCode</code>) becomes a blocking card naming the tool, and the tool is left out of the spec. Studio previously emitted it with an empty backing, which the platform refused outright while the import reported success \u2014 a draft that could not be governed and nothing saying why.</p> <p>To carry a tool across, add it to the spec yourself with a real backing after the import. An n8n MCP client node is the shape Kneo could express directly, but it is not among the node types Studio converts at the pinned n8n@1.62.1, so it imports as a blocking <code>proprietary_node</code> card today.</p> <p>The other <code>ai_tool</code> case is blocking too: n8n's agent as tool pattern, where a sub-agent is attached to a main agent to delegate to. Kneo has no form for it. The sub-agent imports as a separate step with nothing wired to it, and draws three blocking cards \u2014 \"Offered as a tool, imported as a step\", \"The source's trigger never reaches this node\", and a second-entry-point card. The source let the main agent decide whether to call it; decide whether the delegation mattered, then wire it in or remove it.</p> <p>Does not convert. Ordinary processing steps \u2014 <code>httpRequest</code>, <code>code</code>, and anything outside the pinned node list \u2014 each become a blocking <code>proprietary_node</code> gap: \"No Kneo equivalent\". This is the part worth internalising before importing something real:</p> <p>A Kneo spec is agent-centric. An n8n workflow that is mostly plain steps \u2014 fetch, transform, branch, write \u2014 has almost nothing to map onto, and will import as a wall of blocking gaps. That is the importer being honest, not broken. The workflows that convert well are the ones where an agent is doing the work and the other nodes are feeding it.</p> <p>Two partial cases, both blocking:</p> <ul> <li>an <code>if</code> condition is compiled by n8n to an expression Kneo cannot carry, so   the branch shape converts and the condition becomes an <code>expression</code> gap;</li> <li>a credential written into a node \u2014 <code>needs-work.json</code> puts one in an   <code>Authorization</code> header \u2014 becomes an <code>inline_secret</code> gap. The value is not   carried into the spec; bind a credential reference instead.</li> </ul>"},{"location":"examples/#why-needs-workjson-is-here","title":"Why <code>needs-work.json</code> is here","text":"<p>A realistic first import often looks like this one, and seeing three blocking gaps with no reference point reads as failure. It is the honest answer for a workflow with no agent in it: <code>Fetch records</code> and <code>Vendor widget</code> have no Kneo equivalent, and the header carries a secret.</p> <p>Import it after <code>chat-agent.json</code>, so the difference is visible rather than alarming. Its blocking gaps are what stops it exporting \u2014 that gate is the product working.</p> <p>The API key in it is a placeholder (<code>sk-live-NOT-A-REAL-KEY</code>) and matches nothing.</p>"},{"location":"examples/#a-dify-app-difyanswer-workflowyml","title":"A Dify app: <code>dify/answer-workflow.yml</code>","text":"<p>A <code>workflow</code> app in the shape Dify's Export DSL writes (DSL <code>0.7.0</code>): a start form asking for a question, one LLM answering it, and an end node returning the answer. Import it with Source: Dify, or over the API at <code>/import/dify</code>, with the same body and the same alias rules as above:</p> <pre><code>curl -X POST http://127.0.0.1:8091/api/projects/default/import/dify \\\n  -H 'Content-Type: application/json' \\\n  -d \"$(jq -n --rawfile raw examples/dify/answer-workflow.yml '{raw: $raw}')\"\n</code></pre> File Blocking gaps Manual gaps Info notes Converts to <code>dify/answer-workflow.yml</code> none <code>run_input</code> \u00d72 (the start form, the LLM's user message), <code>output_formatting_not_carried</code> (the end node's named output) the start a one-agent spec: the model and the system prompt <p>On Dify the start's card is <code>info</code>, not <code>manual</code>: a start is how a caller begins a run, which a Kneo run already is. What a clean Dify import asks of you instead is run input \u2014 what each caller now supplies \u2014 and the response format. This one is clean because an LLM does all the work. Most real Dify apps are not: over 614 real exports, 607 convert and 43 of those carry no blocking card. What blocks the rest is in Importing a Dify app.</p>"},{"location":"examples/#subsections","title":"Subsections","text":"<ul> <li>n8n/</li> <li>Dify/</li> </ul>"},{"location":"examples/dify/","title":"Dify","text":""},{"location":"examples/dify/#files","title":"Files","text":"<ul> <li><code>answer-workflow.yml</code></li> </ul>"},{"location":"examples/n8n/","title":"n8n","text":""},{"location":"examples/n8n/#files","title":"Files","text":"<ul> <li><code>chat-agent.json</code></li> <li><code>hello-agent.json</code></li> <li><code>agent-with-tool.json</code></li> <li><code>needs-work.json</code></li> </ul>"},{"location":"pub/","title":"Published artifacts","text":"<p>Generated reference artifacts. The prose guides live in the user guides; this section holds the generated outputs only.</p> <ul> <li><code>openapi.json</code> \u2014 the OpenAPI 3 spec for the Studio BFF HTTP surface   (<code>/api/*</code>: both the platform-passthrough routes and the studio-domain import   routes). This is the contract the Studio SPA's typed client is generated   from \u2014 not the Kneo Agent Platform's <code>/v1</code> spec.</li> </ul> <p>For importable sample workflows, see the examples.</p>"},{"location":"pub/#files","title":"Files","text":"<ul> <li><code>openapi.json</code></li> </ul>"},{"location":"user/","title":"User documentation","text":"<p>End-user docs for Kneo Agent Studio \u2014 for developers importing, converting, hardening, and handing off agents as governed Kneo specs.</p> <p>For the top-level docs index see the docs home.</p>"},{"location":"user/#the-policy-amended-080-a9","title":"The policy, amended <code>[0.8.0 A9]</code>","text":"<p>This page used to end with \"the remaining pages are planned; they are written alongside the features they document, cut by cut.\" That was not true, and had not been for several cuts \u2014 features shipped and their pages did not, which is the promise breaking rather than the schedule slipping (<code>docs-user/F-04</code>).</p> <p>Amended rather than met, deliberately. Committing the whole page backlog is a larger piece of work than any cut has had room for, and a promise renewed each cut and broken each cut is worse than an honest floor. So:</p> <ul> <li>The floor is task coverage, not page count. Every shipped task a user can   perform is covered somewhere in the pages that exist, and the reading order   below says where. A task with no section covering it is the defect; a task   with no page of its own is not.</li> <li>A separate page per topic is backlog, not a commitment. Items below marked   covered in are complete against the floor. Items marked planned are   neither written nor promised for a particular cut.</li> <li>A page is written when a feature needs more than a section, which is a   judgement about the material rather than a rule about cuts.</li> </ul> <p>The earlier finding's \"six shipped surfaces have no user page\" is withdrawn as a count: it mixed current and future work and was never measured. The defect that stands is the broken promise, and this is the amendment.</p>"},{"location":"user/#reading-order","title":"Reading order","text":"<ol> <li><code>quickstart.md</code> \u2014 run the BFF + SPA, connect a platform    profile, import your first artifact. Written.</li> <li><code>hosting.md</code> \u2014 running a shared instance: the <code>static</code> and    <code>oidc</code> login modes and their configuration files, admission, and what a    reverse proxy needs. Written. Added at 0.2.1: the login modes shipped in 0.2.0 and their two environment    variables appeared in no published document, so the release's headline    feature could not be configured from anything a user could read.</li> <li><code>debugging.md</code> \u2014 the debug loop: walking the graph, reading    the trace, comparing checkpoints, and stopping a run. Written. It also names what is not built \u2014 replay and resume \u2014 says how the    board behaves when a run finishes, and names the platform's paging depth that    the checkpoint strip and the trace stop at, rather than leaving a reader to    decide whether a puzzling reading is a bug in their spec.</li> <li><code>import_dify.md</code> \u2014 importing a Dify app export: the DSL    versions Studio reads, the support matrix \u2014 every app mode and node type,    converted or carded \u2014 and the cards a Dify import usually carries. New in    0.9.0; 0.8.0's Dify importer was a scaffold that answered    <code>501 not_implemented</code>, and this page was listed below as planned while that    was all there was.</li> </ol> <p>Covered in the pages above, no separate page:</p> <ol> <li>Importing an n8n workflow \u2192 quickstart, which walks a    real import end to end. <code>import_n8n.md</code> is backlog.</li> <li>Reading gap cards, and clearing them \u2014 what \"manual mapping required\"    means, who may acknowledge or attest a card, and what to do when a workflow    cannot be converted at all \u2192 quickstart.    <code>gap_analysis.md</code> is backlog.</li> <li>Exporting and handing off \u2014 the governed spec, and the Hand-off page that    converts it into what the Dashboard's Launch accepts \u2192    quickstart. <code>handoff.md</code> is backlog.</li> <li>Running a shared instance \u2014 login modes, admission, proxies, the analysis    keyring, replicas \u2192 hosting.</li> </ol> <p>Planned, and not promised to a cut:</p> <ol> <li><code>spec_editor.md</code> \u2014 the YAML editor and the platform-rendered graph.</li> <li><code>policy_hardening.md</code> \u2014 tool permissions, guardrails, human-approval, secrets.</li> <li><code>test_run.md</code> \u2014 proving the spec in dev/local.</li> </ol> <p>What this list promises. Items 1\u20138 are complete against the floor above: every task a user can perform today is covered by a section one of these pages actually contains. Items 9\u201311 are backlog \u2014 they are written when the material outgrows a section, not on a schedule. A page appearing here is not a commitment that it will exist next cut; that is the promise this list used to make and kept breaking.</p>"},{"location":"user/#pages","title":"Pages","text":"<ul> <li>Quickstart</li> <li>Importing a Dify app</li> <li>Debugging a run</li> <li>Hosting Studio for a team</li> </ul>"},{"location":"user/debugging/","title":"Debugging a run","text":"<p>You started a test run and something went wrong. This page is the loop for finding out why \u2014 not just that it failed.</p> <p>Everything here is served today. What is not built yet is named at the end, with the two operations that are still missing, followed by how the board behaves when a run finishes and how deep it can page into a very long one.</p>"},{"location":"user/debugging/#open-the-run","title":"Open the run","text":"<p>The Debug page is reached from a run on the Test run page \u2014 its Debug this run link, offered once the run has reached the platform \u2014 or directly at <code>/projects/{project}/debug/{run_id}</code>, where <code>run_id</code> is Studio's run id, not the platform's. It has three panels, and they answer different questions:</p> Panel The question it answers Graph Where did the run get to, and which steps did it visit? Trace What happened, in order, with the messages each step emitted Checkpoints What did the state look like between two steps \u2014 and what changed <p>A platform answer about a different run from the one asked about is refused as <code>502 platform_protocol_error</code>, never shown as this run's record. Since 0.9.0, that check covers the graph, the checkpoint diff, the trace's run status and Stop; 0.8.0 and earlier make it only on the run's own status read.</p>"},{"location":"user/debugging/#walk-the-graph","title":"Walk the graph","text":"<p>The graph shows your spec's steps with the ones the run visited marked, and, while the run is live, the step it is on marked running. A step that was never entered stays plain \u2014 which is usually the first thing worth noticing, because a run that stopped early stopped somewhere, and that somewhere is the step to read first.</p> <p>The header carries the run's own status as the platform reports it \u2014 <code>running</code>, <code>completed</code>, <code>failed</code>, <code>cancelled</code> \u2014 and whether the board is still following the run or it has finished. The node highlight and the header read the same answer, so they agree; When the run finishes, below, says what a finished run looks like.</p>"},{"location":"user/debugging/#read-the-trace","title":"Read the trace","text":"<p>The trace is the ordered record of what each step did. It pages, newest window last, and it is the panel to read when the graph tells you where and you need what.</p> <p>Each page says which events it holds, whether the platform calls the page complete, and how many events the collector lost, when the platform says. While the run is live the trace re-reads the page on screen; it does not follow the tail, so events past that page need Later.</p>"},{"location":"user/debugging/#compare-two-checkpoints","title":"Compare two checkpoints","text":"<p>A checkpoint is the run's state as it stood between two steps. Selecting one shows the full state it held. Selecting two of them shows a diff \u2014 which is the fastest way to find the step that put the wrong value into the state, as opposed to the step that later failed because of it. Those are rarely the same step, and the diff is what separates them.</p>"},{"location":"user/debugging/#stop-a-run","title":"Stop a run","text":"<p>A run that is going nowhere can be stopped from the Debug page. Stopping is final: it is not a pause, and there is no resume (see below).</p> <p>Stop needs the editor role. The button is on the page for every role, and a viewer who presses it is refused. It is a cooperative request: the run stops at its next safe point, and effects that already ran stay done \u2014 a tool call that already ran, already ran. The board shows what the platform answered: that the stop was requested, that the run had already finished, or that it was already cancelled.</p>"},{"location":"user/debugging/#what-is-not-built-yet","title":"What is not built yet","text":"<p>Two operations from the original debug design are not here, and nothing on the page pretends otherwise:</p> <ul> <li>Replay \u2014 a read-only, deterministic replay view of a finished run   (<code>GET /v1/runs/{id}/replay</code>); it re-executes nothing.</li> <li>Resume \u2014 continuing a run that stopped at a human-approval step.</li> </ul> <p>Neither is scheduled to a cut. See the roadmap.</p>"},{"location":"user/debugging/#when-the-run-finishes","title":"When the run finishes","text":"<p>While a run is live the board re-reads the trace, the graph and the checkpoint strip every two seconds. When the trace reports the run finished, all three stop polling \u2014 a finished run's record does not change.</p> <p>They also stop if the trace cannot be read at all \u2014 a stale or pasted run id that answers <code>404</code>, say. A single failed re-read during a live run is shown as an error, and the board keeps following.</p>"},{"location":"user/debugging/#the-board-takes-one-final-read-at-the-end","title":"The board takes one final read at the end","text":"<p>The moment the trace reports the run finished, the board reads the graph and the checkpoint page on screen once more. So the header's finished is never shown beside a graph or a strip from before the end: the last step and the final checkpoint are on screen without a reload. A read that was still in flight from before completion is replaced by that final one, so it cannot land after it.</p>"},{"location":"user/debugging/#a-finished-run-has-nothing-marked-running","title":"A finished run has nothing marked running","text":"<p>A step is marked running only while the run is live. Once the run has finished, the step it stopped on reads visited, like every other step it entered. The canvas, the header and the Stop button all take \"is this run still going\" from the same answer, so they cannot disagree about it. The step the run stopped on is still the one to read first.</p>"},{"location":"user/debugging/#how-deep-the-board-can-page","title":"How deep the board can page","text":""},{"location":"user/debugging/#the-checkpoint-strip-stops-at-the-platforms-paging-depth","title":"The checkpoint strip stops at the platform's paging depth","text":"<p>The platform accepts a checkpoint offset of at most 10,000, and the strip shows checkpoints a hundred at a time, so the deepest page it can show ends at checkpoint 10,100. At that depth Later is disabled and the strip says it has reached as far as the platform pages. When the platform gave a total, it also says how many later checkpoints exist and cannot be read. The trace has the same bound on its events and says so the same way.</p> <p>If a page fails to load, Earlier stays on screen, so a failed page does not strand you.</p> <p>What to do: nothing on the board reaches past this depth \u2014 the bound is the platform's, and Studio refuses a deeper request itself, before asking the platform: an offset past it answers <code>422 invalid_checkpoint_window</code> for checkpoints and <code>422 invalid_trace_window</code> for the trace. That matters only to a script calling the routes directly; the board never asks. Both ceiling checks are new in 0.9.0. On 0.8.0 and earlier neither route checks the ceiling: a deeper offset is passed to the platform, which refuses it with its own validation error. Read the strip's message: it tells you whether what you are comparing is the end of the run or only as far as the platform pages.</p> <p>Earlier releases listed three rough edges of the Debug board here: the board freezing before the run's last update, a finished run's last node labelled \"running\", and a checkpoint page past the ceiling that failed to load. The label was fixed in 0.8.0, and the final read and the paging ceiling in 0.9.0. What remains is the platform's own paging depth, described above.</p>"},{"location":"user/hosting/","title":"Hosting Studio for a team","text":"<p>Studio's default is one developer's laptop, and the quickstart covers that. This page is the other shape: a shared instance that other people sign in to.</p> <p>Until 0.2.1 this page did not exist, and neither <code>KNEO_STUDIO_USERS_FILE</code> nor <code>KNEO_STUDIO_OIDC_PROVIDER_FILE</code> appeared in any published document \u2014 the login modes were 0.2.0's headline feature and could not be configured from anything a user could read (audit M6). Everything below is transcribed from the loaders that enforce it.</p>"},{"location":"user/hosting/#running-the-published-image","title":"Running the published image","text":"<pre><code>docker run \\\n  -p 8091:8091 \\\n  -v kneo-state:/var/lib/kneo-studio/state \\\n  -v kneo-default:/var/lib/kneo-studio/default \\\n  -v kneo-workspace:/var/lib/kneo-studio/workspace \\\n  -e KNEO_STUDIO_AUTH_MODE=static \\\n  -e KNEO_STUDIO_USERS_FILE=/etc/kneo-studio/users.json \\\n  -e KNEO_STUDIO_DEPLOYMENT_CAP=53687091200 \\\n  -e KNEO_STUDIO_ALLOWED_HOSTS=studio.example.com \\\n  -v kneo-secrets:/etc/kneo-studio:ro \\\n  ghcr.io/kneo-agent/kneo-studio:&lt;version&gt;\n</code></pre> <p>It will refuse to start without a login mode and a cap, deliberately. The image binds <code>0.0.0.0</code> so it is reachable at all, and Studio refuses an unauthenticated routable bind \u2014 every request would be the local owner with full rights over the project tree. It also refuses a shared deployment with no cap of its own, because the built-in one is a laptop number chosen by one person for themselves. Both refusals print one line and exit; neither is a bug to work around.</p> Port <code>8091</code>, inside the container and by default outside it State <code>/var/lib/kneo-studio/state</code> \u2014 the store, sessions and the analysis keyring Default project <code>/var/lib/kneo-studio/default</code> \u2014 the attached project bootstrap creates Workspace <code>/var/lib/kneo-studio/workspace</code> \u2014 where members' own projects are allocated <p>The state path doubles, and that surprises people. <code>XDG_STATE_HOME</code> is <code>/var/lib/kneo-studio/state</code>, and Studio puts its own directory inside whatever that names \u2014 so the database is at <code>/var/lib/kneo-studio/state/kneo-studio/studio.db</code>, with <code>kneo-studio</code> appearing twice. That is the XDG convention working as intended (the variable names a base, not an application directory), but it means a backup script pointed at the shorter path copies an empty directory.</p> <p>The three volumes may not nest. The state store inside a project root would make a project directory contain the database describing it, and Studio refuses to start. <code>workspace</code> inside <code>default</code> makes every managed allocation land in the default project's tree, and creation answers <code>409 project_root_overlap</code> for every caller. The image's own paths are already separate; keep them so if you remap.</p>"},{"location":"user/hosting/#bind-mounts-need-chown","title":"Bind mounts need <code>chown</code>","text":"<p>The container runs as uid 10001. A Docker volume is created with the right owner and needs nothing; a bind-mounted host directory arrives owned by whoever owns it on the host, and Studio then cannot create or secure its state directory.</p> <pre><code>chown -R 10001:10001 /srv/kneo-studio/state\n</code></pre> <p>Studio refuses with a short message naming that command rather than a traceback \u2014 but it is still the commonest reason a first <code>docker run</code> stops.</p>"},{"location":"user/hosting/#postgresql-in-the-image","title":"PostgreSQL in the image","text":"<p>The image installs the <code>postgres</code> extra, so <code>KNEO_STUDIO_DB_URL</code> takes a <code>postgresql+psycopg://</code> URL directly:</p> <pre><code>-e KNEO_STUDIO_DB_URL=postgresql+psycopg://kneo:...@db.internal/kneo_studio\n</code></pre> <p>Before 0.7.0 it did not, and the failure was 47 lines of driver traceback with no Studio refusal in it \u2014 the one configuration the \"more than one replica\" advice below requires.</p>"},{"location":"user/hosting/#health","title":"Health","text":"<p><code>GET /api/readyz</code> answers <code>{\"status\": \"ready\", \"store\": true}</code> with <code>200</code>, or <code>503</code> and <code>\"degraded\"</code> when the store cannot be reached. It is unauthenticated and exempt from the <code>Host</code> check so a container health check can reach it; it reports nothing about the deployment beyond those two fields.</p> <p>It deliberately does not check the platform. A Studio whose <code>kneo-serv</code> is unreachable is degraded, not unready \u2014 taking it out of rotation would remove the surface that explains the problem, and restarting it fixes nothing.</p> <p>The image carries a <code>HEALTHCHECK</code> pointed at it already.</p>"},{"location":"user/hosting/#choose-a-login-mode","title":"Choose a login mode","text":"<p><code>KNEO_STUDIO_AUTH_MODE</code> takes one of three values.</p> Mode Who it is for What it needs <code>none</code> (default) one person, on their own machine nothing \u2014 but it is refused on a non-loopback bind <code>static</code> a small team with a file of accounts a users file <code>oidc</code> an organisation with an identity provider a provider file <p><code>none</code> is not a weaker password; it is no authentication \u2014 every request is the local owner. Studio therefore refuses to serve <code>none</code> on a routable address rather than trusting you meant it. A shared instance also has to declare its storage cap.</p>"},{"location":"user/hosting/#static-a-users-file","title":"<code>static</code> \u2014 a users file","text":"<pre><code>export KNEO_STUDIO_AUTH_MODE=static\nexport KNEO_STUDIO_USERS_FILE=/etc/kneo-studio/users.json\nexport KNEO_STUDIO_ALLOWED_HOSTS=studio.example.com   # the name people type\nexport KNEO_STUDIO_DEPLOYMENT_CAP=53687091200         # shared: declare it\n</code></pre> <p>The users file is owner-only, or Studio refuses to start. It holds password hashes, and since 0.5.0 it is held to the same rule as the analysis keyring: a file with any group or other permission bit set \u2014 read, write or execute \u2014 is refused at startup, naming the path, the mode and the fix \u2014 <code>chmod 600</code>. Owner-only means owned by the account Studio runs as; in the published image that is uid <code>10001</code>, so a bind-mounted file needs <code>chown 10001</code> as well as the mode (a <code>0600</code> file the process does not own is refused as unreadable instead). The image can do the hand-over itself, which needs no root on the host:</p> <pre><code>docker run --rm --user 0 -v kneo-secrets:/etc/kneo-studio \\\n  -v \"$PWD/users.json:/tmp/users.json:ro\" --entrypoint sh IMAGE -c \\\n  'cp /tmp/users.json /etc/kneo-studio/ &amp;&amp; chown 10001:10001 /etc/kneo-studio/users.json &amp;&amp; chmod 600 /etc/kneo-studio/users.json'\n# then mount the volume read-only: -v kneo-secrets:/etc/kneo-studio:ro\n</code></pre> <p><code>KNEO_STUDIO_ALLOWED_HOSTS</code> is not optional here, and it is the first thing that goes wrong: a request whose <code>Host</code> is not on the list is refused with <code>403 host_not_allowed</code> before it reaches the login route, so a mistyped or missing entry looks like a broken sign-in rather than a configuration error. Studio's Host allowlist is a DNS-rebinding defence and does not read your proxy's mind. (Found by running this page's own instructions \u2014 the first draft omitted it, and every login 403'd.)</p> <p>The file is JSON: either a list of users, or an object with a <code>users</code> list.</p> <pre><code>{\n  \"users\": [\n    {\n      \"username\": \"ada\",\n      \"password_hash\": \"$argon2id$v=19$m=65536,t=3,p=4$\u2026\",\n      \"display_name\": \"Ada Lovelace\"\n    }\n  ]\n}\n</code></pre> <ul> <li><code>username</code> and <code>password_hash</code> are required; <code>display_name</code> defaults to the   username.</li> <li><code>password_hash</code> must be an argon2id hash. A plaintext password is   refused, not accepted with a warning \u2014 \"the failure mode is a file of   plaintext passwords in a backup\".</li> <li>A username appearing twice is refused, because which password worked would   otherwise depend on file order.</li> <li>An empty list is refused: <code>static</code> with no users authenticates nobody.</li> </ul> <p>All of these are checked at startup, not at first login. An instance that cannot authenticate anyone should say so before someone tries to sign in.</p> <p>What startup checks of the hash is its <code>$argon2id$</code> prefix, and nothing more <code>[0.9.0 Phase-5 docs audit]</code>. A hash that starts correctly but is truncated or mangled \u2014 a copy-paste that lost its tail \u2014 loads without a word, and that account then answers <code>401 invalid_credentials</code> to its correct password, which looks exactly like a wrong one. If one user cannot sign in and the others can, re-mint that user's hash before suspecting the password.</p> <p>Failed sign-ins are throttled per account and per address, and password checks run a bounded number at a time; the numbers, the codes and the settings are in the quickstart's Login attempts.</p>"},{"location":"user/hosting/#producing-a-hash","title":"Producing a hash","text":"<p><code>argon2-cffi</code> is already installed as a dependency, so the interpreter running Studio can mint one:</p> <pre><code>python -c 'from argon2 import PasswordHasher; import getpass; \\\nprint(PasswordHasher().hash(getpass.getpass(\"password: \")))'\n</code></pre> <p><code>getpass</code> rather than an argument, so the password does not land in your shell history.</p> <p>Hash every password with the same parameters \u2014 mint them all with the same command, as above. A sign-in for an unknown username is checked against a decoy hash, so that its timing does not reveal which usernames exist. Since 0.9.0, that decoy is built with the configured users' argon2 parameters; when the file holds more than one parameter set it takes the costliest and logs a warning, because login time can then tell an account with a cheaper hash from an unknown username. 0.8.0 and earlier always build the decoy with <code>argon2-cffi</code>'s defaults, which the command above also uses \u2014 one more reason to mint every hash with it.</p>"},{"location":"user/hosting/#oidc-a-provider-file","title":"<code>oidc</code> \u2014 a provider file","text":"<pre><code>export KNEO_STUDIO_AUTH_MODE=oidc\nexport KNEO_STUDIO_OIDC_PROVIDER_FILE=/etc/kneo-studio/provider.json\n</code></pre> <p>The provider file holds the client secret, and the rule stated for the users file above applies unchanged: owner-only (<code>chmod 600</code>, owned by the account Studio runs as) or Studio refuses to start, since 0.5.0.</p> <pre><code>{\n  \"provider_id\": \"corp-idp\",\n  \"issuer\": \"https://login.example.com/tenant-id/v2.0\",\n  \"client_id\": \"\u2026\",\n  \"client_secret\": \"\u2026\",\n  \"callback_origin\": \"https://studio.example.com\",\n  \"authorization_endpoint\": \"https://login.example.com/tenant-id/oauth2/v2.0/authorize\",\n  \"token_endpoint\": \"https://login.example.com/tenant-id/oauth2/v2.0/token\",\n  \"jwks_uri\": \"https://login.example.com/tenant-id/discovery/v2.0/keys\",\n  \"redirect_allowlist\": [\n    \"https://studio.example.com/api/session/oidc/callback\"\n  ],\n  \"admission\": {\n    \"mode\": \"allowlist\",\n    \"email_domains\": [\"example.com\"]\n  },\n  \"bootstrap\": {\n    \"owner\": {\"kind\": \"oidc\", \"issuer\": \"https://login.example.com/tenant-id/v2.0\", \"subject\": \"\u2026\"}\n  }\n}\n</code></pre> <p>Required: <code>provider_id</code>, <code>issuer</code>, <code>client_id</code>, <code>client_secret</code>, <code>callback_origin</code>, the three endpoints <code>authorization_endpoint</code>, <code>token_endpoint</code> and <code>jwks_uri</code>, a non-empty <code>redirect_allowlist</code>, and an <code>admission</code> block.</p> <p>This example was missing the last four until 2026-08-15, so an operator who copied it got <code>ProviderConfigError</code> and no bound port. The page claimed to have been written by running it; that was true of the <code>static</code> path and not of this one. The block above now loads \u2014 checked by feeding it to <code>load_provider()</code>, not by reading the loader.</p> <p>There is no <code>discovery</code> shortcut. This release does not fetch <code>.well-known/openid-configuration</code>; naming a <code>discovery</code> key is refused outright rather than ignored, because a metadata document silently overriding what you configured is worse than being told to write three URLs. Copy them from your provider's published metadata.</p> <p><code>redirect_allowlist</code> must contain the derived callback URI exactly \u2014 <code>&lt;callback_origin&gt;/api/session/oidc/callback</code>. Registering it with your provider is necessary and not sufficient: Studio builds its own redirect from <code>callback_origin</code>, checks that it is in this list, and refuses to start if it is not. That catches a <code>callback_origin</code> typo at startup instead of at somebody's first sign-in, where it surfaces as the provider's opaque <code>redirect_uri_mismatch</code> and sends you to the wrong console.</p> <p>It is a startup cross-check and nothing else. Studio never accepts a redirect target from a request, so any entry beyond the derived URI is inert \u2014 listing three does not permit three. Nothing here is what stops an open redirect; the post-login return path is bounded separately, by <code>safe_return_to</code>, which admits only same-origin paths.</p> <ul> <li><code>issuer</code> is the exact <code>iss</code> your provider stamps into tokens, and it is   required because it cannot be derived. Providers that scope the issuer per   tenant (Azure AD among them) publish an issuer that is not the URL you   configured discovery with, and a mismatch is refused at verification.</li> <li><code>callback_origin</code> is where the browser lands, and Studio builds the   redirect URI as <code>&lt;callback_origin&gt;/api/session/oidc/callback</code>. Register that   exact URI with the provider.</li> <li>Every configured URL is checked for transport before it is used, and a   configured endpoint whose own query would shadow a protocol parameter   (<code>state</code>, <code>nonce</code>, <code>code_challenge</code>, <code>grant_type</code>, \u2026) is refused at load   rather than at the first login.</li> <li>Signing keys are cached for five minutes and fetched with a 15-second   timeout, in a worker thread, so a slow key endpoint delays the sign-in that   needs it and nobody else. Rotating a key at the provider needs no restart: a   token signed with a new <code>kid</code> triggers a fresh read at once; a key you remove   from the set keeps verifying for at most those five minutes.</li> <li>The ID token must be signed with an asymmetric algorithm: <code>RS256</code>,   <code>RS384</code>, <code>RS512</code>, <code>ES256</code>, <code>ES384</code> or <code>ES512</code>, and, since 0.9.0, <code>PS256</code>,   <code>PS384</code> or <code>PS512</code>. <code>none</code> and the <code>HS*</code> family   are refused, since an HMAC \"signed\" with the provider's published key would   verify. Any other algorithm fails the sign-in as <code>provider_error</code>, with the   reason in Studio's log; on 0.8.0 and earlier that includes a provider signing   with RSA-PSS.</li> </ul>"},{"location":"user/hosting/#admission-who-may-exist","title":"<code>admission</code> \u2014 who may exist","text":"<p>A valid token is not permission to have an account, so there is no default; the block is required.</p> <code>mode</code> Meaning <code>allowlist</code> only identities matching a rule may become principals; a matching identity becomes an active principal at its first sign-in <code>provision</code> any identity the provider authenticates becomes a pending principal, which an operator approves with <code>kneo-studio principal approve</code> <p>Allowlist rules \u2014 <code>hosted_domains</code>, <code>email_domains</code>, <code>groups</code>, <code>subjects</code> \u2014 are lists, and an identity matching any one rule is admitted. Each reads one claim of the verified ID token <code>[0.9.0 Phase-5 docs audit]</code>:</p> Rule Reads <code>hosted_domains</code> <code>hd</code> <code>email_domains</code> the domain of <code>email</code>, only when the token also carries <code>email_verified: true</code>; an unverified or absent flag never matches <code>groups</code> <code>groups</code>, or <code>roles</code> when there is no <code>groups</code> claim <code>subjects</code> <code>sub</code> <p><code>hosted_domains</code>, <code>email_domains</code> and <code>groups</code> are matched case-insensitively. <code>subjects</code> is matched exactly, because an OIDC <code>sub</code> is a case-sensitive opaque identifier: <code>Ada</code> and <code>ada</code> are different accounts, and Studio must not fold one into the other.</p> <p>The allowlist is checked at every sign-in, not only the first. An active principal whose claims no longer match \u2014 removed from the group at the IdP, or outside a rule you narrowed \u2014 is refused its next login.</p> <p>Two refusals are worth knowing before you hit them: <code>allowlist</code> with no rules is refused (it admits nobody, and \"empty means allow everything\" is the other defensible reading, which is exactly why you may not have to guess), and <code>provision</code> with rules is refused rather than ignoring them, because an operator who wrote both believes they apply.</p>"},{"location":"user/hosting/#signing-in-is-two-steps-and-the-first-one-fails","title":"Signing in is two steps, and the first one fails","text":"<p>Authenticating is not the same as being allowed to exist. In <code>static</code> mode, and in <code>oidc</code> under <code>provision</code>, the first sign-in registers the identity as pending, and an operator admits it <code>[0.9.0 Phase-5 docs audit \u2014 this said \"</code>oidc<code>under</code>allowlist<code>\", which is backwards]</code>. Under <code>static</code> a correct password gets you:</p> <pre><code>403 not_admitted\nthis account is awaiting admission; an operator approves it with\n`kneo-studio principal approve`\n</code></pre> <p>Under <code>oidc</code> the sign-in is a browser redirect, so there is no <code>403</code>: the browser is sent back to the sign-in page with <code>?error=not_admitted</code>, which reads \"Your account is not admitted to this instance yet.\" Under <code>oidc</code> <code>allowlist</code> there is no pending step: an identity that matches a rule is an active principal at its first sign-in, and one that matches none is sent back with the same <code>not_admitted</code> and no principal is created (the refusal is audited) \u2014 there is nothing to approve, and the fix is the allowlist.</p> <p>That is the design, not a fault. Walked through, for <code>static</code>:</p> <pre><code>$ kneo-studio principal list\nactive    operator     host\npending   ada          kneo-studio:static\nactive    local-owner  local\n\n$ kneo-studio principal approve --subject ada --as \"your name\"\napproved ada (kneo-studio:static)\n</code></pre> <p>Now the same credentials return <code>200</code>. <code>--as</code> is a label recorded on the audit row for the human running the command; it is never treated as identity. <code>--issuer</code> is needed only when one subject exists under more than one issuer.</p> <p>(This section exists because running the page's own instructions produced a 403 where the draft promised a login. A hosting guide that has not been executed is a guess.)</p>"},{"location":"user/hosting/#becoming-the-owner","title":"Becoming the owner","text":"<p>A fresh shared instance has no owner, and approving a principal does not make one. One way per login mode <code>[0.9.0 Phase-5 docs audit]</code>:</p> <ul> <li><code>oidc</code>: <code>bootstrap.owner</code> in the provider file names <code>{kind, issuer,   subject}</code> in advance; that login becomes the owner. It is the only way an   <code>oidc</code> instance gains an owner, because the OIDC callback carries no claim   token. All three fields are required and <code>kind</code> must be <code>\"oidc\"</code>; a partial or   mistyped block is refused at startup (<code>ProviderConfigError</code>), not left to   match nothing. <code>subject</code> is taken exactly as written, case and surrounding   whitespace included: it must equal the <code>sub</code> your provider signs, byte for   byte. A non-string value is refused rather than converted. Under <code>allowlist</code>   the owner adopts at their first sign-in; under <code>provision</code> that first sign-in   is pending, and they adopt at the next one, after you approve them.</li> <li><code>static</code>: <code>kneo-studio bootstrap-token</code> mints a one-time claim. It is   printed once; paste it into the sign-in form's I have an adoption token   field on the sign-in that should become the owner, before that person   signs in for the first time \u2014 a sign-in without it registers them as pending,   and a pending account cannot adopt. The command refuses under <code>oidc</code>.</li> </ul> <p>Both <code>principal</code> and <code>bootstrap-token</code> are audited host commands: they run on the machine, never through the API.</p> <p>Adoption is one-way. It disables the local owner, which is the identity every request is under <code>auth_mode = none</code>, so an adopted instance restarted under <code>none</code> has nobody to act as. <code>GET /api/me</code> answers <code>503</code> (\"no active principal\"), and, since 0.9.0, so do <code>GET</code> and <code>POST /api/projects</code>. On 0.8.0 and earlier those two routes still act for the disabled local owner: listing its projects, and creating new ones owned by and charged to it. Keep an adopted instance on <code>static</code> or <code>oidc</code>.</p> <p>Set <code>KNEO_STUDIO_PROJECT_ROOT</code> before the first host command</p> <p>The default project is attached to a directory the first process to open the store picks, and host commands open the store. With the variable unset that directory is whatever shell you happened to run the command from \u2014 and whoever adopts the instance becomes the owner of that tree and can read files out of it.</p> <p>Export it alongside the other settings, in the same shell you run <code>bootstrap-token</code> from and in the service's own environment:</p> <pre><code>$ export KNEO_STUDIO_PROJECT_ROOT=/srv/kneo/projects\n</code></pre> <p>A shared instance refuses to start without it rather than guessing, and refuses to move an existing project to a different tree \u2014 the people granted access were granted it against the old one. To work with a second tree, attach it as its own project with <code>project attach</code>.</p>"},{"location":"user/hosting/#removing-somebody","title":"Removing somebody","text":"<p>Deleting someone's entry from <code>users.json</code> is not enough. Studio reads that file only when somebody signs in. Removing an entry refuses their next login, and a session they already hold keeps working until it expires. To end their access now, disable them as below, which also ends their sessions.</p> <p>Under <code>oidc</code> the same holds for the identity provider. Removing somebody's account at the IdP refuses their next login, and so, under <code>allowlist</code>, does removing them from the group or domain it admits, because the allowlist is checked at every sign-in. Neither touches a session they already hold: Studio receives no event from the IdP. <code>principal disable</code> is what ends a live session.</p> <p>New in 0.3.0. Disabling a principal ends every session it holds and refuses it a login, in one transaction:</p> <pre><code>$ kneo-studio principal disable --subject ada --as \"your name\"\ndisabled ada; 1 session(s) ended, 0 pending invitation(s) revoked\n</code></pre> <p>It also revokes every pending invitation that principal issued, and says how many.</p> <p>If that person is the only active owner of a project, or the project's storage is charged to them, the command refuses and names what is blocking it \u2014 because a project with no active owner has nobody who can add one, and a charge on a disabled principal is a bill nobody can move. Name who inherits and the whole thing lands together:</p> <pre><code>$ kneo-studio principal disable --subject ada\nerror [sole_owner]: 'p-ada' is the only active owner of proj-7; name a principal to inherit ownership\n$ kneo-studio principal disable --subject ada --transfer-to grace --as \"your name\"\ndisabled ada; 1 session(s) ended, 0 pending invitation(s) revoked\n  ownership of proj-7 -&gt; grace\n  charge for proj-7 -&gt; grace (48213 bytes)\n</code></pre> <p><code>--transfer-to</code> covers both moves, which is nearly always what you want: whoever created a project is usually both its owner and the account it is charged to. Use <code>--owner-to</code> and <code>--quota-to</code> when they should go to different people.</p> <p>Who can inherit: an active member of that project. This is deliberate \u2014 the command overrides who may decide, never who is involved, and handing a project to a stranger gives them something they cannot see. Add them as a member first.</p> <p>If a write is in progress, the charge will not move and you will see <code>[quota_transfer_busy]</code>. Wait and retry: the writer's lease expires and the reservation is reconciled, which is bounded. Forcing the move would land the bytes on whichever side finished last.</p>"},{"location":"user/hosting/#when-the-owner-is-already-gone","title":"When the owner is already gone","text":"<p>Two break-glass commands for the case where there is nobody to ask \u2014 no session, no password, no cooperation:</p> <pre><code>$ kneo-studio project reassign-owner --project proj-7 --to grace --as \"your name\"\n$ kneo-studio project reassign-quota --project proj-7 --to grace --as \"your name\"\n</code></pre> <p>Both are operator-only and audited, like every host command. <code>reassign-owner</code> promotes without removing anyone: guaranteeing the project has an active owner is one decision, and removing the old one is another that <code>principal disable</code> handles. <code>reassign-quota</code> is the only path that moves a charge without the target agreeing and without disabling anyone \u2014 <code>principal disable --quota-to</code> or <code>--transfer-to</code> also moves one without asking, as part of the disable.</p>"},{"location":"user/hosting/#behind-a-reverse-proxy","title":"Behind a reverse proxy","text":"<p>Four settings <code>[0.9.0 Phase-5 docs audit \u2014 this said \"three\"]</code>. A login mode is required behind a proxy (from 0.3.0), and <code>KNEO_STUDIO_TRUSTED_PROXIES</code> is what lets sign-in through a forwarding proxy succeed (from 0.7.0):</p> <pre><code>export KNEO_STUDIO_ALLOWED_HOSTS=studio.example.com\nexport KNEO_STUDIO_HOST=127.0.0.1        # proxy talks to loopback\nexport KNEO_STUDIO_TRUSTED_PROXIES=127.0.0.1   # the address the proxy connects FROM\nexport KNEO_STUDIO_AUTH_MODE=static      # or oidc \u2014 see above\n</code></pre> <p><code>KNEO_STUDIO_TRUSTED_PROXIES</code> takes IP addresses and CIDR blocks, not hostnames. Set it to the address Studio sees your proxy coming from \u2014 <code>127.0.0.1</code> for a same-host proxy, the container-network address for a sidecar. Studio believes <code>X-Forwarded-*</code> headers, or an RFC 7239 <code>Forwarded</code> header, only from a peer on this list, which is why the setting exists: without it any caller could claim to be a proxy. If your proxy sends both and they disagree, Studio believes neither.</p> <p>With it set, the session cookie is marked <code>Secure</code> when your proxy reports <code>X-Forwarded-Proto: https</code> (or <code>Forwarded: proto=https</code>), and only then: arriving on a routable interface is not evidence that anything was encrypted, and 0.7.0 stopped treating it as such. Keep <code>Strict-Transport-Security</code> at the proxy \u2014 Studio does not send it.</p>"},{"location":"user/hosting/#forgetting-it-stops-being-silent-in-070","title":"Forgetting it stops being silent in 0.7.0","text":"<p>Until 0.7.0 an authenticated instance behind a proxy with no policy started, served and issued session cookies without <code>Secure</code> \u2014 silently, on a connection that was HTTPS all the way to the browser. From 0.7.0 that configuration is refused at sign-in: any request carrying forwarding headers from a peer the policy does not name answers <code>403 untrusted_forwarding</code> and says which variable to set.</p> <p>Signing in is refused; nothing else is. Sessions already established keep working, and every other route keeps exactly the checks it had.</p> <p>The variable stays optional, and unset, blank and empty are one state \u2014 no proxy is trusted:</p> <pre><code>KNEO_STUDIO_TRUSTED_PROXIES=            # same as not setting it at all\n</code></pre> <p>There is no keyword for \"no proxy\", deliberately. This field takes IP addresses and CIDR blocks \u2014 a hostname is refused, because it resolves to something else at some later moment \u2014 and a magic word inside a field of addresses is a word that could one day collide with a host somebody named. If nothing forwards to your instance, leave the variable out; a container needs no new setting to run.</p> <p>If you set the variable correctly and sign-in is still refused, the peer Studio sees is not your proxy \u2014 check the launch command below.</p> <p>Start Studio with <code>kneo-studio</code>, as the container does. The console script starts the server so that Studio, not the server, reads the forwarding headers. Launching the application with a bare <code>uvicorn kneo_studio.app:app</code> command line turns uvicorn's own header handling back on: it rewrites the peer address from <code>X-Forwarded-For</code> before Studio sees it, <code>KNEO_STUDIO_TRUSTED_PROXIES</code> then never matches, the cookie loses <code>Secure</code>, and a caller on the same host can pick its own login-throttle key with one header. That launch is not supported.</p>"},{"location":"user/hosting/#if-your-proxy-setup-stopped-working-when-you-upgraded","title":"If your proxy setup stopped working when you upgraded","text":"<p><code>auth_mode = none</code> behind a proxy is refused from 0.3.0, and this is the one upgrade in this release that can stop a working deployment. Two shapes:</p> <ul> <li>Studio will not start: <code>KNEO_STUDIO_TRUSTED_PROXIES</code> is set while   <code>KNEO_STUDIO_AUTH_MODE</code> is <code>none</code> (or unset, which means <code>none</code>).</li> <li>Requests come back <code>403 unauthenticated_proxy</code>: no policy is configured, but   requests are arriving with forwarding headers, so something is proxying them.</li> </ul> <p>Both mean the same thing. Under <code>auth_mode = none</code> every request is the local owner with no credential required, so a proxy in front of Studio published the owner's API \u2014 including writes into the project tree \u2014 to everyone who could reach the proxy. It was reported as a security defect against 0.2.1 and fixed rather than warned about, because the guarantee on the security page is written unconditionally.</p> <p>The fix is to choose one: set <code>KNEO_STUDIO_AUTH_MODE=static</code> or <code>oidc</code> and keep the proxy, or take the proxy out of the path and reach Studio directly on loopback. If you are sure no proxy is involved, something in front of Studio is adding <code>X-Forwarded-*</code> or <code>Forwarded</code> headers; stop it sending them.</p> <p>The full rules are the transport trust policy.</p>"},{"location":"user/hosting/#when-an-owner-asks-you-to-raise-a-taint-cap","title":"When an owner asks you to raise a taint cap","text":"<p>Studio bounds how much analysis provenance a project may hold. An owner who runs into one of those bounds sees their usage and headroom on the project's limits screen and then has to ask you, because a cap an owner could raise themselves is not a cap.</p> <pre><code>kneo-studio quota set-taint-cap --project alpha --project-cap 20000\nkneo-studio quota set-taint-cap --project alpha --per-principal 2000 --retained-max 200000\n</code></pre> <p>Any one flag alone, or several together. There are three bounds, and they answer different questions:</p> flag bounds notes <code>--project-cap</code> live associations the project holds superseded ones excluded, so repairing is free <code>--per-principal</code> live associations any one member holds so one editor exhausts their own share, not the project's <code>--retained-max</code> every association ever recorded cannot go below the rows already retained, and those are never evicted, so in practice it only rises <p>Each is refused below what the project already holds, with that number in the message, so a refusal tells you what you can set. <code>--per-principal</code> is checked against the busiest member rather than the total, since a value that is fine on average can already be violated for one person.</p> <p><code>--retained-max</code> has its own flag rather than following <code>--project-cap</code> automatically: a bound that moved as a side effect of changing a different bound is the kind of coupling you would discover by surprise. If a project keeps hitting it, that is churn \u2014 a repair loop or a heavily upgraded analyzer \u2014 not a project holding too much, and the refusal code says which.</p> <p>Every change is audited with the old and new values for all three.</p>"},{"location":"user/hosting/#attaching-a-directory-that-already-exists","title":"Attaching a directory that already exists","text":"<p>Members can create projects, and when they do Studio chooses the path \u2014 beneath <code>KNEO_STUDIO_WORKSPACE_BASE</code>, named after the allocated project id. The client sends a name and never a path, which is what makes it safe for anyone to call: there is no input to traverse with, and collisions cannot happen.</p> <p>Set <code>KNEO_STUDIO_WORKSPACE_BASE</code>, or that capability does not exist. There is no default \u2014 on a laptop the single project is the developer's own attached checkout, so nothing needs one \u2014 and an unset value is not a half-configured state but a disabled feature: <code>POST /api/projects</code> refuses with <code>409 project_creation_disabled</code>, and a member who tries gets a refusal about server configuration they cannot change.</p> <pre><code>-e KNEO_STUDIO_WORKSPACE_BASE=/var/lib/kneo-studio/workspace\n</code></pre> <p>The published image sets it already (and gives it its own volume). This paragraph used to describe the allocation as though the variable were always configured, which is true of the image and of nothing else.</p> <p>Not the same directory as <code>KNEO_STUDIO_PROJECT_ROOT</code>. Every managed allocation would then land inside the default project's tree, and creation would answer <code>409 project_root_overlap</code> for every caller, always.</p> <p>Pointing Studio at a tree that already exists is a different proposition. An arbitrary directory may hold credentials, another team's work, or your <code>/etc</code>, and a project root is a directory Studio then reads, writes, exposes to the project's members and hashes into an exposure digest. So it is a host command, never an API route, and it works only against a list you declare:</p> <pre><code>export KNEO_STUDIO_ATTACHABLE_ROOTS=/srv/teams,/srv/archive\nkneo-studio project attach --path /srv/teams/payments --owner ada\n</code></pre> <ul> <li>Comma-separated absolute directories. Any subtree of a listed directory may   be attached, and a listed directory may be attached itself. A relative entry is   refused rather than resolved against whatever directory Studio was started   from, and a path containing a comma cannot be expressed.</li> <li>Unset means the command is disabled, and says so. There is no default,   for the same reason <code>KNEO_STUDIO_WORKSPACE_BASE</code> has none: a guess here would be   a guess about which of your files Studio may read and hand to somebody else.</li> <li>The allowlist is about the filesystem, not the string you typed. A symlink   inside a listed directory pointing somewhere outside it is refused.</li> <li><code>--owner</code> takes a subject who already exists \u2014 they get the owner role and   the project's storage charge. Overlap with any existing project's root is   refused in both directions: one tree belongs to one project.</li> </ul> <p>Every attach writes an audit row naming the resolved path, because which tree was attached is the question anyone reviewing it later will have.</p>"},{"location":"user/hosting/#registering-a-file-when-nobody-can","title":"Registering a file when nobody can","text":"<p>Registering a pre-existing file is an owner action, and it does not happen in the browser: Studio's UI offers no register control on purpose. An owner registers through the API route, and the host command below covers the cases the route cannot \u2014 because adding a manifest entry widens what sharing the project exposes, and the flow makes the owner accept the whole aggregate rather than one file at a time.</p> <p>That leaves the case where there is no owner able to ask \u2014 an attached tree whose owner has gone, or one you registered before inviting anybody. The host fallback is deliberately two commands:</p> <pre><code>kneo-studio draft register --project alpha --path agent.yaml\n# prints the root identity, the current exposure digest, what is already\n# registered, and exits without changing anything\n\nkneo-studio draft register --project alpha --path agent.yaml \\\n    --exposure-digest 4f3c\u2026\n</code></pre> <p>The second run refuses if the digest has moved since you were shown it, which is what stops your registration slipping past somebody else's confirmation. The entry is attributed to the operator, never to a Studio user \u2014 a host command has no session, and the manifest is not a table to put a guess in.</p>"},{"location":"user/hosting/#what-a-shared-instance-still-owes-you","title":"What a shared instance still owes you","text":"<ul> <li>A declared storage cap \u2014 <code>KNEO_STUDIO_DEPLOYMENT_CAP</code>; Studio refuses to   start shared without one (why).</li> <li>A project-count bound per principal \u2014 <code>KNEO_STUDIO_PROJECT_LIMIT</code>, which   defaults to 100 live projects and may be set to any positive whole number,   higher or lower; zero, a negative or a non-number refuses to start. Unlike the storage cap   this does not refuse to start when unset: a byte cap is a resource one   principal can exhaust for everyone, while a project count is per-principal, so   an unset limit degrades to a generous default rather than a shared cliff.</li> </ul> <p>Which projects count: those where the principal is the quota   principal \u2014 the one charged for the bytes \u2014 not the ones they created and not   the ones they are a member of. A project has no singular owner, so counting by   membership would multiply-count a project with two owners. A charge moves by   accepted transfer or by operator reassignment, and the count follows it.</p> <p>What is refused at the bound: creating a project, accepting a transfer into   a full principal, an operator reassignment or a bulk <code>principal disable</code> that   would move an estate into one, and <code>project attach</code>. The refusal is   <code>409 project_limit_reached</code> and it names both the count and the limit, because   the remedy depends on which of the two is wrong.</p> <p>What is never refused: an estate that is already over the limit. A   bootstrapped first project and an adopted estate are grandfathered \u2014 an   inherited over-limit principal starts and signs in normally, and only its next   acquisition is refused. Raising the limit is always safe; lowering it below   what principals already hold does not retroactively break them. - A state store outside every project root, which Studio also enforces. - Backups of three things, not two. The project directories, the database,   and <code>$XDG_STATE_HOME/kneo-studio/analysis-keyring.json</code>.</p> <p>An artifact's governance history lives in the database, so project directories   alone are not a backup. But the analysis payloads in that database are sealed   with AES-256-GCM, and the key is not in it \u2014 <code>load_keyring(state_home())</code>   resolves the keyring from the state home independently of where the database   lives. On the default SQLite deployment they sit side by side and copying the   directory gets both. Under the PostgreSQL option below they are in different   places by construction, and an operator who backs up Postgres plus the   project directories has everything except the key.</p> <p>Restoring without it is not a degraded instance, it is a stopped one: the   keyring is validated against the live rows before serving, so Studio refuses   to start and the sealed payloads are unrecoverable. This bullet named two   things until 2026-08-15.</p>"},{"location":"user/hosting/#what-refuses-to-start-and-why","title":"What refuses to start, and why","text":"<p>One rule, stated once: when configuration is missing, contradictory or unreadable, Studio refuses to start rather than guess. Every such refusal is a <code>StartupRefusal</code>; <code>kneo-studio serve</code> prints its message as one line \u2014 what is wrong, and what to set \u2014 and exits non-zero, with no traceback. Nothing below is a degraded mode. The causes, by the setting that fixes them:</p> Refusal Setting A shared instance with no project root, while no default project has been attached yet; or one asked to move an existing default project to a different tree. Shared means any of: a login mode other than <code>none</code>, a non-loopback <code>KNEO_STUDIO_HOST</code>, <code>KNEO_STUDIO_WORKSPACE_BASE</code> set, or a principal other than the local owner in the store <code>KNEO_STUDIO_PROJECT_ROOT</code> A shared instance (as above) with no storage cap, or a cap that is not a whole number greater than zero <code>KNEO_STUDIO_DEPLOYMENT_CAP</code> A project limit that is not a whole number greater than zero <code>KNEO_STUDIO_PROJECT_LIMIT</code> A login mode other than <code>none</code>, <code>static</code> or <code>oidc</code> <code>KNEO_STUDIO_AUTH_MODE</code> Login mode <code>none</code> on a non-loopback bind <code>KNEO_STUDIO_HOST</code>, <code>KNEO_STUDIO_AUTH_MODE</code> A port that is not a number <code>KNEO_STUDIO_PORT</code> <code>KNEO_STUDIO_TRUSTED_PROXIES</code> set while the login mode is <code>none</code> <code>KNEO_STUDIO_AUTH_MODE</code> A <code>KNEO_STUDIO_TRUSTED_PROXIES</code> entry that is not an IP address or CIDR block, such as a hostname <code>KNEO_STUDIO_TRUSTED_PROXIES</code> A state directory Studio cannot create or secure \u2014 the bind-mount case in Bind mounts need <code>chown</code>, whose message runs to a few lines because it carries the command <code>XDG_STATE_HOME</code>, or the directory's owner A state store inside the project root <code>XDG_STATE_HOME</code>, <code>KNEO_STUDIO_PROJECT_ROOT</code> A <code>static</code> users file or <code>oidc</code> provider file that is missing, unreadable, malformed, or has any group or other permission bit set (<code>chmod 600</code>, owned by the account Studio runs as \u2014 uid <code>10001</code> in the image) <code>KNEO_STUDIO_USERS_FILE</code>, <code>KNEO_STUDIO_OIDC_PROVIDER_FILE</code> A recovery interval or lease that is zero, negative or unreadable <code>KNEO_STUDIO_RECOVERY_INTERVAL_SECONDS</code>, <code>KNEO_STUDIO_RECOVERY_LEASE_SECONDS</code> A key id that cannot be read back, no active key id whenever <code>KNEO_STUDIO_ANALYSIS_KEYS</code> is set (even with a single key), an active key id that names none of the configured keys, or a keyring that is world- or group-readable or does not open the live rows <code>KNEO_STUDIO_ANALYSIS_KEYS</code>, <code>KNEO_STUDIO_ANALYSIS_ACTIVE_KEY_ID</code> A store whose schema the migrations cannot read, or an analysis-key rotation left in a state the code cannot continue from none \u2014 the message names the recovery command <p>The sections that describe each setting mention its refusal where it matters to the reader; this table is the definition, and the rule does not vary by row. (Until 0.5.0 four of these were described one at a time, each as if it were its own kind of failure \u2014 the 0.4.0 audit's deferral \"one statement would cover them\", pulled forward.)</p>"},{"location":"user/hosting/#test-run-recovery","title":"Test-run recovery","text":"<p>A test run that Studio dispatched but never heard back about \u2014 the process died mid-call, or the response was lost \u2014 is settled by a background task the server owns: one pass at startup, then one every <code>KNEO_STUDIO_RECOVERY_INTERVAL_SECONDS</code> (default 60). It asks the platform whether a run with the dispatch's session exists and adopts exactly one match; every other answer settles the run as <code>unknown</code>. It never re-issues a run \u2014 a duplicate LLM run is worse than an unresolved status line.</p> <p><code>KNEO_STUDIO_RECOVERY_LEASE_SECONDS</code> (default 300) is how long one worker's claim on a run lasts. Two Studio processes over one PostgreSQL store will both see a stale run; the lease is what stops them both settling it, and a worker that dies mid-recovery parks its run for this long before another may take it over. Set it longer than a correlation should ever take and shorter than you would wait for a run's status.</p> <p>Both must be positive numbers of seconds. A zero, negative or unreadable value refuses to start rather than silently falling back to the default \u2014 a lease of <code>0</code> is a fence any second worker walks straight through, and an operator who typed it meant something.</p>"},{"location":"user/hosting/#postgresql","title":"PostgreSQL","text":"<p><code>KNEO_STUDIO_DB_URL</code> points the state store at PostgreSQL instead of the default SQLite file \u2014 the escape hatch ADR-013 exists for, and the reason a shared instance can outgrow a single file. Install the extra and set the URL:</p> <pre><code>pip install 'kneo-studio[postgres]'\nexport KNEO_STUDIO_DB_URL=postgresql+psycopg://user:pass@host/kneo_studio\n</code></pre> <p>The schema is created and migrated by Studio itself on first start, the same migrations the SQLite default runs. Remember the keyring: see the bullet above \u2014 this is exactly the deployment where the database and the key stop being in the same place.</p>"},{"location":"user/hosting/#more-than-one-replica","title":"More than one replica","text":"<p>Read this first. Single-instance shared hosting is the supported baseline \u2014 one Studio process, any number of people signing in to it. Running several replicas needs everything below and one condition this page did not state until 0.8.0: every replica's project mounts must report the same device and inode, not merely the same contents.</p> <p>A confirmed exposure is pinned to the project root's device number, inode and canonical path. A replica whose mount disagrees \u2014 which is the ordinary case for two hosts mounting one NFS export \u2014 refuses content reads on every project whose exposure was confirmed elsewhere, saying \"this project's root has changed since its exposure was confirmed\". Nothing has changed; that replica is simply somewhere else, and the message cannot tell the difference.</p> <p>So: a shared database and shared storage are necessary and are not sufficient. Several processes on one host see the same identity and are fine. Across hosts, verify it before relying on it \u2014 <code>stat -c '%d %i'</code> on the project root from each replica must agree \u2014 and treat a disagreement as unsupported rather than as something to work around. Loosening the check is not the workaround: it is what stops a re-pointed root inheriting someone else's confirmation.</p> <p>A single instance generates its analysis key on first run and keeps it beside the state store with owner-only permissions. Two replicas cannot each do that \u2014 they would write payloads the other cannot read \u2014 so the key comes from the environment instead, and both replicas must be given the same one:</p> <pre><code>export KNEO_STUDIO_ANALYSIS_KEYS='k1:&lt;base64 32 bytes&gt;'\nexport KNEO_STUDIO_ANALYSIS_ACTIVE_KEY_ID=k1\n</code></pre> <ul> <li><code>KNEO_STUDIO_ANALYSIS_KEYS</code> is a comma-separated list of   <code>&lt;key-id&gt;:&lt;base64 material&gt;</code> entries. Setting it replaces the on-disk keyring   entirely; the file is not consulted. A key id is 1\u201364 characters and may not   contain <code>.</code> (it delimits the stored envelope's fields), <code>,</code> or <code>:</code> (they   delimit this variable's entries) or control characters; anything else \u2014   <code>tenant@prod</code>, <code>k/1</code> \u2014 is fine. Studio refuses to start on an id it could not   read back rather than sealing payloads under it.</li> <li><code>KNEO_STUDIO_ANALYSIS_ACTIVE_KEY_ID</code> names the key used for writes, and is   required rather than defaulting to \"the only one\" \u2014 a deployment that later   added a second key would otherwise silently change which key writes.</li> <li>Startup refuses, rather than degrading, when the keyring is incomplete: if a   stored payload names a key id the keyring does not carry, Studio does not start.   That is deliberate \u2014 reaching a per-row decision with an incomplete keyring is   the one thing that must not happen, since a missing key is a configuration fault   and corrupt ciphertext under a complete keyring is a data fault, and they need   different answers.</li> </ul> <p>The login bounds are per process, not per deployment. The failed-attempt throttle, the OIDC start limit and the number of password checks run at once (Login attempts) are each held in one process's memory, so N replicas allow N times each limit. Size them per replica, or put a rate limit at the load balancer if you need one bound across the estate.</p>"},{"location":"user/hosting/#upgrading-studio-stop-the-estate-first","title":"Upgrading Studio \u2014 stop the estate first","text":"<p>A schema upgrade requires a quiescent estate. Stop every Studio process before upgrading, and start them again only on the new version.</p> <p>This is a precondition, not advice. Studio already refuses to start against a store newer than the running version \u2014 \"state store is at schema version N, but this Studio supports at most M\" \u2014 so a replica you forget to upgrade fails closed on its next restart. What that refusal cannot do is stop a process already running when the migration lands: it holds an open connection and never re-checks, so it can keep writing under the old release's assumptions.</p> <p>Two replicas starting the new version at the same moment is safe, though <code>[0.6.0]</code>. Their first starts serialize on a database lock: one applies each pending migration and seeds the default rows, the other waits, sees the work already done, and comes up clean. Before this, a simultaneous first start could crash the loser on a duplicate-key error and leave you restarting it by hand. You still stop the old processes first; you no longer have to stagger the new ones.</p> <p>For most migrations the cost of that is a lost write. For the analysis keyring it is worse and permanent: an old process can create a payload under a key the upgrade is retiring, and retiring the key then makes that payload unreadable. So the requirement is stated here rather than left to inference.</p> <p>One part of it is now enforced rather than requested <code>[0.4.0]</code>: a schema migration refuses to run while an analysis-key rotation session is open, naming the session and telling you to complete or cancel it first. Nothing is applied and the store is unchanged. That closes the worst case \u2014 a migration rebuilding the very table that holds the rotation's ownership record, so that afterwards nothing could tell a completed rotation from an interrupted one. It does not replace stopping the estate: a running old process is still invisible to the store, which is why the paragraphs above are a precondition and not a suggestion.</p> <p>If you run a single instance on a laptop, stopping it is what you were going to do anyway. If you run replicas, take them all down together \u2014 a rolling upgrade is not supported for schema changes.</p> <p>What the 0.6.0 upgrade changes in a 0.5.0 store, and why <code>[0.6.0]</code>. One data migration runs alongside the schema changes: it reclaims the gap detail of every 0.5.0 analysis whose parameters not carried card could quote a credential. That card listed the model options an import could not carry \u2014 <code>temperature</code>, <code>maxTokens</code> and the rest \u2014 and in 0.5.0 it quoted their values verbatim, including an <code>apiKey</code> the importer had already identified as a secret and refused to carry anywhere else. So a 0.5.0 store can hold a credential in review content, encrypted at rest and readable by any viewer of the project. After the upgrade, such an analysis keeps its blocking and manual counts and its acknowledgements, and opening its detail answers gap detail was reclaimed; re-import to see it again \u2014 the repair the product already names for a reclaimed payload. Re-importing the same export produces a fresh analysis whose card shows the option's name and <code>&lt;redacted&gt;</code>. Analyses of workflows with no model options, and every analysis written by 0.6.0, are untouched.</p> <p>A credential a pre-0.6.0 import may have written into a draft <code>[0.6.0]</code>. Before 0.6.0, if a workflow repeated one of its own credentials in an agent's system prompt, its model identifier, or a node name, Studio carried that value into the generated spec \u2014 so it was returned by the import API and written into a plaintext draft under the project root, beside a gap card stating the value had not been carried. 0.6.0 does not carry it, cards it as a blocking gap, and refuses the import outright if any other position still would.</p> <p>No migration can undo the ones already written, and none is attempted: a generated draft is an artifact's governed base, and rewriting it would move bytes an artifact's revision pins. What to do, in this order:</p> <ol> <li>Search your project roots for the secret, not for the workflow:    <code>grep -rIl '&lt;the credential&gt;' /path/to/projects</code>. Drafts are plaintext YAML.</li> <li>Treat any hit as a disclosed credential and rotate it. It was written to disk    unencrypted, so it is in your filesystem backups and in anything that read them;    deleting the file does not undo that.</li> <li>Delete the affected draft and re-import the workflow. 0.6.0's analyzer produces    the blocking card, so the re-import tells you which position to fix in the source.</li> <li>Artifacts built on such a draft keep pointing at it. Retract or recreate them after    the re-import, or the artifact's own snapshot still carries the value.</li> </ol> <p>One thing the upgrade cannot repair for you <code>[0.6.0]</code>. Before 0.6.0, an agent that carried both a system message and a per-run prompt (<code>promptType: define</code> with <code>text</code>) was analysed with its system prompt and without the per-run prompt becomes run input card, so such an artifact could be handed off with no acknowledgement and no word about the request it dropped. No migration can find those analyses \u2014 the defect is a card that is absent, inside an encrypted payload \u2014 so an artifact created before the upgrade keeps the analysis it has. Re-import the workflow: 0.6.0's analyzer produces the card, hand-off then waits for the acknowledgement, and the exported spec carries the system prompt as before.</p> <p>Disable supervisors and autoscalers for the whole window, not just until the last old process exits. The refusal above can only fire once the schema version has actually advanced, so anything that restarts a process during the migration can bring an old binary back against a store that has not finished moving. systemd <code>Restart=</code>, a container orchestrator's replica count, or an autoscaler will all do exactly that. Turn them off, upgrade, then turn them back on.</p> <p>Added 2026-08-24 by maintainer decision, after an external design review found that this page documented no upgrade precondition while the design assumed one.</p>"},{"location":"user/hosting/#rotating-the-analysis-key","title":"Rotating the analysis key","text":"<p>You can, from 0.4.0 \u2014 and the route is a sequence of deliberate steps rather than an environment variable, because a rotation that begins by changing configuration is one that can be half-applied across a fleet. Configuration still has to follow the store, though, and when is the whole runbook <code>[corrected 2026-09-05, 0.6.0 S4]</code>.</p> <p>The rule the sequence is built around. Every process is configured with a preferred write key, <code>KNEO_STUDIO_ANALYSIS_ACTIVE_KEY_ID</code>, and the store checks it for intent at every startup and every admission refresh \u2014 it never uses it; the store chooses the write identity. What the check accepts depends on the rotation phase:</p> Store phase <code>KNEO_STUDIO_ANALYSIS_ACTIVE_KEY_ID</code> may name stable (no session) only the active id prepared, or rewriting the source or the target any phase never a retired id <p>A process whose configured id is outside that set refuses: it exits non-zero at startup, and a running one fails its next analysis access with <code>503</code>. So the moment to move the configured id is while the session is open \u2014 prepared or rewriting \u2014 when both answers are accepted and each replica can be rolled one at a time. Move it after <code>complete</code> and the store is stable on the new id while every replica still names the old, retired one: the whole estate refuses at once.</p> <pre><code>$ kneo-studio keys status          # phase, active id, and what live rows still name\n$ kneo-studio keys prepare --target new-key\nprepared. session &lt;id&gt;\nEvery process must now hold BOTH keys. Writes stay on the old id until `keys cut-over`.\n#   1. put BOTH keys in KNEO_STUDIO_ANALYSIS_KEYS on every replica (restart each)\n$ kneo-studio keys cut-over --session &lt;id&gt;\n#   2. now roll KNEO_STUDIO_ANALYSIS_ACTIVE_KEY_ID=new-key across the replicas, one\n#      at a time \u2014 the store is in a phase that accepts either id, so nothing refuses\n$ kneo-studio keys rewrite --session &lt;id&gt;   # repeat until it reports nothing left to move\n$ kneo-studio keys complete --session &lt;id&gt;  # LAST: only once every replica names new-key\nretired the source. \u2026\n#   3. only now remove old-key from KNEO_STUDIO_ANALYSIS_KEYS\n</code></pre> <p>The four-step form of this runbook \u2014 with no mention of the configured id \u2014 was executed verbatim on a two-replica deployment during 0.6.0's Phase-1 audit: after <code>complete</code>, every import and every analysis read answered <code>503</code> and every restart exited non-zero, until the operator worked out which variable to change. The same rotation with the configured id rolled during the rewrite window was measured at zero interruption, both replicas serving <code>201</code> through cut-over and rewrite. Nothing in the product changed; the runbook had left out the step that decides the outcome.</p> <p>There is no initialization step: a store upgraded from an earlier release adopts the key it is already using automatically at startup (<code>keys init</code> exists as the idempotent operator surface, and a fresh store initializes itself the same way). The one store startup will not adopt is a mixed-id predecessor \u2014 live rows under more than one key \u2014 which is refused unchanged with the way out named.</p> <p>Keep the session id. <code>cut-over</code>, <code>complete</code> and <code>cancel</code> all require it, and none of them infers which rotation you meant from anything else \u2014 that inference is what made an earlier attempt unable to tell \"released\" from \"still held\".</p> <p>Both keys must be in every process's keyring between <code>prepare</code> and <code>complete</code>. That is what <code>prepare</code> is for: it makes the new key mandatory before anything writes under it, so a process that has not been given the key fails its next analysis access rather than discovering the gap halfway through.</p> <p><code>complete</code> refuses until every live payload names the new key and retires the old one in the same transaction \u2014 there is no step that proves the old key is unreferenced and then separately releases it, because that gap is a window in which the proof stops being true. Quarantined analyses are counted and excluded: they carry no live key dependency, so a store that has had a corrupt analysis can still retire a key.</p> <p>Cancelling is safe only when the configured id agrees with what stays active. Rows already rewritten stay rewritten, nothing is retired, and <code>keys resume --source old-key</code> picks the rotation back up \u2014 that part holds in either phase. But a cancel ends the session, and a stable store accepts only its active id: cancel while prepared and the old key stays active, so a replica already rolled to the new id refuses; cancel while rewriting and the new key stays active, so a replica still on the old id refuses. Before cancelling, set every replica's <code>KNEO_STUDIO_ANALYSIS_ACTIVE_KEY_ID</code> to the id the cancel will leave active \u2014 or resume instead of cancelling. This paragraph said \"safe at any point\" until 2026-09-05; it was false in both cancellable phases.</p> <p>Before 0.4.0 this section said \"you cannot, in this release.\" Changing <code>KNEO_STUDIO_ANALYSIS_ACTIVE_KEY_ID</code> on a store holding sealed payloads made the service refuse to start. That refusal still exists and is still right \u2014 configuration is not how a rotation is performed \u2014 but it is no longer the end of the road.</p> <p>Why configuration alone was never going to be enough. Reads work across a mixed store already \u2014 every payload records the key that sealed it \u2014 so the problem was never reading. It was that writes would begin under the new key while old rows kept naming the old one, with no way to rewrite them, so the keyring had to keep holding a key you rotated away from because it leaked, indefinitely, with nothing in the product saying so. The startup refusal existed to stop you reaching that state. Rotation is what finally gets you out of it.</p> <p>Where each key lives is unchanged:</p> <ul> <li>on a single-instance install the first key is generated on first run and kept in the   state home;</li> <li>on a hosted deployment it is whatever <code>KNEO_STUDIO_ANALYSIS_KEYS</code> names, and during a   rotation that variable must name both keys \u2014 until <code>complete</code> has committed,   never earlier: between a cancel and a <code>resume</code> the old key is still authorized and   still named by live rows, so admission requires it, and removing it refuses the   estate with the missing id named.</li> </ul> <p>This capability was built for 0.3.0 and withdrawn from it, after five rounds of external implementation review each found defects in the previous round's fixes and the fifth recommended withdrawal rather than a sixth attempt. Nothing from that attempt was ever released, so no store carries a trace of it. What ships in 0.4.0 was rebuilt against a written state-transition contract \u2014 the thing the withdrawn attempt lacked, and the reviewer's actual diagnosis: there was no model, so each round repaired the instance in front of it.</p> <p>One thing you should know about that contract: it was reviewed extensively but never confirmed by a party with no stake in it, because this project has no such party available. That is recorded as an explicit waiver rather than glossed, and what stands in for the missing read is executable evidence \u2014 the acceptance controls the release lane runs.</p>"},{"location":"user/import_dify/","title":"Importing a Dify app","text":"<p>Studio converts a Dify app export \u2014 the YAML file Dify's Export DSL writes \u2014 into a Kneo <code>version: v1</code> spec plus gap cards, the same way it converts an n8n workflow. What converts, what is carded and why is below; the quickstart walks the rest of the pipeline \u2014 review, govern and hand-off \u2014 which is the same for every source.</p> <p>Dify import is new in 0.9.0. 0.8.0 and earlier answer a Dify import with <code>501 not_implemented</code>.</p>"},{"location":"user/import_dify/#import-it","title":"Import it","text":"<ol> <li>In Dify, open the app and choose Export DSL. Dify exports the app's draft, so unpublished    edits are what Studio converts. Keep the secret values out of the export when Dify asks: Studio    never carries a credential, and a literal one only adds a blocking card.</li> <li>In Studio, choose Import, pick Dify as the source, and choose the <code>.yml</code> or <code>.yaml</code>    file. Over the API it is <code>POST /api/projects/{project}/import/dify</code>, with the same body and    alias rules as an n8n import (the examples    show both, and carry a Dify app to try first).</li> <li>Read the gap cards. Blocking ones must be fixed in the draft and attested before the spec    can be exported; manual ones need an acknowledgement; info ones only tell you.</li> </ol>"},{"location":"user/import_dify/#which-exports-studio-reads","title":"Which exports Studio reads","text":"<p>Studio is verified against Dify 1.17.1, whose DSL is version <code>0.7.0</code>. It converts every <code>0.x</code> DSL version up to <code>0.7.0</code>, including those below <code>0.1.0</code> \u2014 the versions Dify itself imports without asking for confirmation \u2014 and reads an export with no version as <code>0.1.0</code> (absent, <code>null</code> or empty alike), as Dify does. A newer version (a newer <code>0.x</code>, or another major such as <code>1.0.0</code> or <code>v1</code>), a version that is not a string, and one Studio cannot parse are refused, with a message naming which: Studio does not guess at a format it has not verified.</p>"},{"location":"user/import_dify/#the-support-matrix","title":"The support matrix","text":"App mode Studio What it becomes <code>workflow</code> converted the graph: one Kneo step per node Kneo can run, the rest carded <code>advanced-chat</code> converted the graph, as for <code>workflow</code>; each LLM's conversation memory is carded <code>chat</code> converted one Kneo agent: the model and the prompt <code>agent-chat</code> converted one Kneo agent: the model and the prompt. Its tools and its iteration bound are carded for you to set: a Dify tool has no backing Kneo can call, and the bound is not carried. An app with no strategy, or a router strategy, runs no agent in Dify and is carded, blocking <code>completion</code> converted one Kneo agent: the model and the prompt <code>agent</code> carded nothing: its prompt and tools live in the agent package the export carries under <code>agent_packages</code>, which Studio does not read <p>Inside a graph, a node becomes:</p> Dify node In Kneo <code>start</code>, and the schedule, webhook and plugin triggers a start \u2014 Kneo runs are created by a caller, so a trigger's schedule or route is carded to recreate outside Kneo <code>llm</code>, <code>parameter-extractor</code> an agent that answers once; its prompt is the system prompt, its user messages are run input. Its model is the workflow's shared model variable when its model selector names one; a selector Dify cannot resolve is carded <code>source_cannot_run</code>, blocking <code>agent</code> on Dify's own <code>function_calling</code> or <code>ReAct</code> strategy an agent with its model and instruction; each enabled tool is carded, blocking (see Tools below); any other strategy is carded, blocking <code>if-else</code>, <code>question-classifier</code> graph edges, one per case, each condition carded, blocking, for you to write. A branch straight after the start has no step before it to branch from: only its first case is connected, and every other case's step is carded <code>branching_shape</code>, blocking a node's error handling (a fail branch, a default value) or its retry carded, blocking: a Kneo step fails the run. A fail branch's route is kept as an ordinary edge so there is something to repair <code>answer</code>, <code>end</code> nothing \u2014 flow passes through. What the caller received is carded, manual, whenever a Kneo run's bare reply is not it: a formatted answer, named outputs, an empty answer, an End with no outputs, and an answer of one reference such as <code>{{#sys.query#}}</code>. The one exception is an answer that is exactly <code>{{#&lt;step&gt;.text#}}</code> for the LLM or agent step just before it, with nothing around it and nothing after it, and no other answer able to reply in the same run: Dify sends the caller every answer a run reaches. That step must also be the last the run takes, since a Kneo run returns its last step's output. A terminal missing its <code>answer</code> or <code>outputs</code> field is one Dify cannot build: carded, blocking <code>iteration</code>, <code>loop</code> and everything inside them one carded region, blocking \u2014 never flattened into steps that would run once <code>code</code>, <code>http-request</code>, <code>tool</code> carded, blocking: Kneo cannot run an inline body, an HTTP call or a tool as a step <code>knowledge-retrieval</code>, <code>template-transform</code>, <code>variable-aggregator</code>, <code>assigner</code>, <code>document-extractor</code>, <code>list-operator</code>, <code>human-input</code>, the knowledge-pipeline nodes, Dify's new Agent node (in the export, <code>type: agent</code> with <code>version: \"2\"</code> and <code>agent_node_kind: dify_agent</code>; the editor titles it Agent, like the strategy agent, and its prompt, model and tools live in the agent package under <code>agent_packages</code>), and any node type Studio does not recognise carded, blocking: no Kneo equivalent a canvas note not imported, info <p>The whole mapping, row by row and read from Dify's own source, is the Dify mapping table.</p>"},{"location":"user/import_dify/#the-cards-a-dify-import-usually-carries","title":"The cards a Dify import usually carries","text":"<ul> <li>The model's settings (<code>completion_params</code>) \u2014 <code>manual</code>. The temperature and token cap are   listed with the Kneo field each belongs in; set them by hand or accept the provider's defaults.</li> <li>Run input \u2014 <code>manual</code>. A start form's variables, and an LLM's user message, are what each   caller must now supply. Kneo runs take their input from the caller.</li> <li>A prompt with a variable \u2014 <code>blocking</code>. <code>{{#node.variable#}}</code> in a workflow, <code>{{name}}</code> in a   chat or completion app, and any Jinja template are Dify's own syntax. Kneo does not interpolate   them, so the prompt is not carried; the card quotes it for you to rewrite.</li> <li>Conversation memory \u2014 <code>blocking</code>. Every <code>chat</code> and <code>agent-chat</code> app carries it, because Dify   always sends such an app its history, and so does any <code>advanced-chat</code> LLM, parameter extractor or   agent node that has memory set. A Kneo agent does not keep earlier turns.</li> <li>A model provider Studio does not recognise \u2014 <code>manual</code>. The card names it; set   <code>agent.model.provider</code> and <code>agent.model.name</code> yourself.</li> <li>Tools \u2014 <code>blocking</code>. A Kneo tool needs a backing \u2014 Python, an MCP server or another agent \u2014   that no export can carry, so someone with platform access has to supply it.</li> <li>A fan-out \u2014 <code>blocking</code>, <code>branching_shape</code>. A node whose output is wired to several steps, or   a branch case wired to several, runs them all in Dify. So does a step wired straight on beside a   branch: it runs with whichever case fires. A Kneo graph step follows one edge, the first that   matches, so the rest would never run. The edges are kept for you to rebuild as one path, and   writing the branch's conditions alone does not clear this card. One step per case is a choice,   not a fan-out, and draws no card. Dify runs a step that several connections reach once, after   all of them, so a start wired to <code>a</code> and to <code>b</code> beside <code>a \u2192 b</code> converts as the chain <code>a \u2192 b</code>.   But if the converted workflow reaches <code>b</code> only through a branch, it is carded.</li> <li>The app's features \u2014 one card for each feature the app turns on, because a Kneo spec has no   app features.</li> <li>Moderation (content moderation, <code>sensitive_word_avoidance</code>) is <code>blocking</code> in a chat,     agent-chat, completion or advanced-chat app. Dify refuses a flagged query and replaces a flagged     answer, and the converted agent would do neither, so rebuild the protection first.</li> <li>File upload and text to speech are <code>manual</code>. So are the chat features of an advanced-chat     app: the opener, the follow-up suggestions and speech to text.</li> <li>A feature the app's mode never uses is <code>info</code>, and so are citations beside a knowledge     retrieval.</li> </ul>"},{"location":"user/import_dify/#what-to-expect","title":"What to expect","text":"<p>Over 614 real Dify exports, 607 convert, and 43 of them \u2014 about 7 in 100 \u2014 carry no blocking card. All 43 are <code>workflow</code> or <code>advanced-chat</code> apps in which an LLM does the work. No <code>chat</code>, <code>agent-chat</code> or <code>completion</code> export converts without one: every chat app carries the memory card, and most carry a prompt variable. The blockers you will meet most are: - nodes with no Kneo equivalent (<code>code</code>, <code>http-request</code>, knowledge retrieval, templates); - prompts and conditions written in Dify's variable syntax; - conversation memory; - tool steps; - branches Kneo cannot hang from a step.</p> <p>A clean import shows an <code>info</code> card for the start \u2014 on Dify a start is how a caller begins a run, which a Kneo run already is \u2014 and <code>manual</code> cards for run input and the response format, plus one for each app feature it turns on. <code>examples/dify/answer-workflow.yml</code> is one to compare against.</p>"},{"location":"user/quickstart/","title":"Quickstart","text":"<p>Run Studio, point it at a Kneo Agent Platform, and import your first workflow.</p> <p>Scope <code>[0.9.0]</code>. Import, from n8n or Dify \u2192 govern \u2192 edit \u2192 run \u2192 export: you can change a spec in Studio, run it on the platform and read the result back, and take the governed result out as YAML \u2014 on your own machine or on a shared host under a login mode. The Hand-off page converts the export into the inline JSON the Dashboard's Launch accepts, with the paste steps beside it, and reports up front whether the target environment can actually receive it. The analysis key can be rotated out rather than kept forever (see hosting). The debug loop is mostly here <code>[0.7.0]</code>. A run's graph, its trace, its checkpoints and their diff are served and reachable from the Debug page, and a run can be stopped. What arrives later is replay and resume, and pushing to the Dashboard without the paste. See the roadmap; this page grows with them.</p> <p>This block listed all six debug operations as \"what arrives later\" \u2014 including the four that shipped in 0.7.0 \u2014 and carried no cut stamp, so it read as current scope on the page a new user starts from <code>[docs-consistency/F-01, fixed 0.8.0 A1]</code>. It is the second time this banner has outlived the code; the first is recorded below. <code>test_the_scope_block_does_not_defer_what_ships.py</code> now holds it against the routes that answer.</p> <p>This said \"0.4.0 scope\" and \"what arrives later is test runs \u2014 <code>POST \u2026/test-runs</code> is published and answers a <code>501</code>\" until <code>[2026-09-02]</code>, after the route began dispatching. It was the start page telling readers that the release's headline feature did not exist (touchpoint 2 round 1, F-03).</p>"},{"location":"user/quickstart/#run-it","title":"Run it","text":"<pre><code>pip install kneo-studio\nkneo-studio\n</code></pre> <p>Studio serves the BFF and the built SPA on <code>http://127.0.0.1:8091</code>.</p> <p>Which Studio this page describes. <code>pip install kneo-studio</code> installs the latest release, 0.9.0, which is what this page describes. On 0.8.0 and earlier, <code>examples/n8n/chat-agent.json</code> draws a blocking <code>proprietary_node</code> card for its chat trigger, and a Dify import answers <code>501 not_implemented</code>.</p> <p>Both halves of that address are configurable \u2014 <code>KNEO_STUDIO_HOST</code> (default <code>127.0.0.1</code>) and <code>KNEO_STUDIO_PORT</code> (default <code>8091</code>). The port is worth knowing if something else already owns 8091; the host is subject to the rule in the next paragraph.</p> <p>It will not start on a routable address without authentication. Under <code>auth_mode = none</code> \u2014 still the default \u2014 every request is the local owner with full rights over your working directory, so binding <code>0.0.0.0</code> would publish exactly that. Studio refuses rather than warns, and names the remedy: bind loopback, or set <code>KNEO_STUDIO_AUTH_MODE</code> to <code>static</code> or <code>oidc</code>. Those modes shipped in 0.2.0; the refusal is not a placeholder for them, it is what keeps the local default local.</p> <p>The working directory is the project root. Point somewhere else with <code>KNEO_STUDIO_PROJECT_ROOT</code>.</p>"},{"location":"user/quickstart/#behind-a-reverse-proxy","title":"Behind a reverse proxy","text":"<p>Studio checks the <code>Host</code> and <code>Origin</code> of every request, so a proxy that presents a different name than the one Studio binds is rejected until you say the name is expected:</p> <code>KNEO_STUDIO_ALLOWED_HOSTS</code> comma-separated host names to accept in addition to the loopback defaults <code>KNEO_STUDIO_CORS_ORIGINS</code> comma-separated origins allowed to call <code>/api</code> from a browser; empty by default, which allows none <code>KNEO_STUDIO_TRUSTED_PROXIES</code> the addresses your proxy connects from; without it, a forwarded client address is not trusted \u2014 see hosting <p>Neither widens what the loopback rule above permits: with <code>auth_mode = none</code>, Studio still refuses to bind a routable address. They are documented here because they exist and are enforced today \u2014 this page described neither, and a proxy hop failing a <code>Host</code> check with no setting to reach for is the kind of thing you should not have to read the source to solve.</p> <p><code>KNEO_STUDIO_CORS_ORIGINS</code> is needed today, by anyone running the frontend dev server. <code>npm run dev</code> serves the SPA from <code>localhost:5174</code> and proxies <code>/api</code> to the BFF, so the browser's <code>Origin</code> is the dev server while the request arrives at <code>127.0.0.1:8091</code>. The same-origin rule refuses that mismatch on every mutating call \u2014 import, artifact creation and every other <code>POST</code>/<code>PUT</code>/<code>PATCH</code>/<code>DELETE</code> answer <code>403 cross_origin_refused</code> while reads work fine, which is a confusing way to discover it. Export is a <code>GET</code>, so it keeps working: the check applies to mutating methods only.</p> <p>This listed export among the refused calls until <code>[2026-09-06]</code>, in the same sentence that says the rule fires on mutating ones \u2014 a list that refuted itself, on the page that is the only place a reader learns this boundary.</p> <pre><code>KNEO_STUDIO_CORS_ORIGINS=http://localhost:5174 kneo-studio\n</code></pre> <p>(This paragraph said these settings \"matter once <code>static</code> or <code>oidc</code> land and a served instance becomes startable\" \u2014 presenting a variable the documented development workflow needs now as one for later. The <code>README</code>'s own <code>npm run dev</code> line pointed at the workflow without mentioning it. Two pages each defensible alone, and jointly a dead end.)</p> <p><code>KNEO_STUDIO_RELOAD=1</code> restarts the BFF on source changes. It is a development convenience and belongs nowhere near a shared host.</p>"},{"location":"user/quickstart/#connect-it-to-a-platform","title":"Connect it to a platform","text":"<p>Studio talks to <code>kneo_serv</code> through a <code>kneo_client</code> profile. If you already have one, skip to naming it below.</p> <p>A profile is a section in <code>~/.config/kneo/client.toml</code>:</p> <pre><code>[laptop]\nurl = \"https://kneo.example.com\"\napi_key = \"\u2026\"\nauth_scheme = \"bearer\"   # or \"kneo_api_key\" for the X-Kneo-Api-Key header\ntimeout = 30.0\n</code></pre> <p>Or, without a file at all \u2014 the environment is read after the file and overrides it, so this is a complete profile too:</p> <pre><code>export KNEO_URL=https://kneo.example.com\nexport KNEO_API_KEY=\u2026\n</code></pre> <p>Going the environment route, the profile name is informational: with no <code>client.toml</code>, any name resolves to those variables. Pick one you will recognise in a log.</p> <p>Then name it for Studio:</p> <pre><code>export KNEO_STUDIO_DEFAULT_PROFILE=laptop     # a kneo_client profile name\nexport KNEO_STUDIO_DEFAULT_PLATFORM_ENVIRONMENT=dev   # optional, defaults to dev\nkneo-studio\n</code></pre> <p>That is the whole binding. The profile supplies the URL and the credential; Studio never stores either, and the profile name itself never reaches the browser.</p> <p>Without it, Studio still runs. The project is created unbound: you can import workflows and read the generated spec and its gap cards \u2014 all of that is local. Acknowledging a card needs an artifact, and an artifact needs the binding. What needs a platform is governance (validate, compile, policy report), and the UI says the project is unbound rather than failing at the first attempt.</p> <p>Starting unbound is not a dead end. Set <code>KNEO_STUDIO_DEFAULT_PROFILE</code> and start Studio again; the project picks up the binding on that start, and drafts you already imported become governable. Nothing is lost and nothing needs deleting \u2014 you are back on the path above.</p> <p>The reverse is deliberately not automatic. A project that is already bound is never silently retargeted by an environment change, because its alias generation is a snapshot the artifacts governed under it depend on. Changing a live binding is an operator action at the host \u2014 the same bar as editing the profile store \u2014 and it is <code>env rotate</code>:</p> <pre><code>kneo-studio env rotate --project default --alias default --profile corrected\n</code></pre> <p>That adds a generation rather than moving one, so it goes live for new work immediately while an artifact frozen at the old generation still resolves the binding it was governed against. Once nothing is running against the old one, <code>kneo-studio env retire --project default --alias default --generation N</code> stops it resolving for new work. The full family is <code>env add|rotate|retire|rebind</code>; <code>--help</code> on each explains what it takes.</p> <p>This has been here since 0.3.0, and until it landed a mistyped profile name bound a project permanently \u2014 nothing wrote the retirement column, so there was no way to correct a binding short of editing the state database. The previous advice here was to retarget \"before you have artifacts worth keeping\". You no longer have to.</p>"},{"location":"user/quickstart/#what-the-zero-config-binding-grants","title":"What the zero-config binding grants","text":"<p>Deliberately, as little as possible. The alias is created as <code>default</code> with transport <code>remote</code>, and nothing else is enabled:</p> Out of the box Transport <code>remote</code>, always Host allowlist empty Secret-ref namespace none Operator registry empty <p>A <code>kneo_client</code> profile carries only a name, URL, key, scheme and timeout \u2014 it cannot express a transport or a server root. So there is nothing to infer <code>shared_filesystem</code> from, and Studio does not guess: inferring it would hand a spec filesystem reach on the strength of an assumption.</p> <p>The practical consequence, which is the same under either transport (and has been since 0.2.0):</p> <ul> <li>Path-free specs and overlays work. Layering happens in memory, so an   overlay never needs to be a path the platform can see.</li> <li>Filesystem literals are refused \u2014 <code>skills[].source</code>, <code>mcp_servers.*.cwd</code>,   <code>ca_bundle</code>, <code>client_cert</code>, <code>client_key</code>. There is no bounded dependency   closure yet to materialise a skill tree into, so a spec that names a local   path would be asking the platform to read a file that is not there. The   closure is a later design-first release \u2014 it needs directory semantics,   size limits, traversal rules, quotas and staging decided together \u2014 so no cut   is promised for it; see the roadmap.</li> <li>Secret references pass through untouched. <code>kneo_serv</code> resolves them on its   own host, against its own secret material. Studio never expands one \u2014 on a   remote deployment the value does not exist here at all.</li> <li>Anything that executes needs an operator's approval. An MCP <code>command</code> is   arbitrary process execution on the platform host, so it is permitted only as   <code>{\"$registry\": \"&lt;name&gt;\"}</code> naming an entry the operator approved for that   alias. An empty registry \u2014 the default \u2014 approves nothing.</li> </ul> <p>Registry entries make a spec non-portable: they resolve against this environment, and Studio refuses to export an artifact that depends on one.</p>"},{"location":"user/quickstart/#where-studio-keeps-things","title":"Where Studio keeps things","text":"<p>Two places, and they are deliberately separate:</p> <code>$KNEO_STUDIO_PROJECT_ROOT</code> (default: the working directory) your specs, overlays and imported drafts, under <code>.kneo-studio/</code> \u2014 files you edit, commit and back up like source <code>$XDG_STATE_HOME/kneo-studio/</code> (default <code>~/.local/state/kneo-studio/</code>) <code>studio.db</code>, the snapshot store and the analysis keyring (<code>analysis-keyring.json</code>) \u2014 Studio's own state: projects, principals, analyses, artifacts, audit <p>The database is not in the project root, which surprises people two ways: copying a project directory does not carry its artifacts, and running Studio in a different directory still finds the same projects. Point <code>XDG_STATE_HOME</code> somewhere else to keep instances separate, and include that directory when you back up \u2014 an artifact's governance history lives there, not beside the YAML.</p> <p>Studio refuses to start if the state store is inside the project root, which would make a project directory contain the database describing it.</p>"},{"location":"user/quickstart/#storage","title":"Storage","text":"<p>Studio caps how much it will hold. On a laptop that is 5 GiB, built in \u2014 you do not have to choose a number, and Studio still cannot fill your disk by accident. Override it with <code>KNEO_STUDIO_DEPLOYMENT_CAP</code> (bytes).</p> <p>On anything shared, the cap must be declared and Studio refuses to start without it. \"Shared\" means any of: <code>KNEO_STUDIO_AUTH_MODE</code> other than <code>none</code>, a non-loopback <code>KNEO_STUDIO_HOST</code>, a configured <code>KNEO_STUDIO_WORKSPACE_BASE</code>, or a principal other than the local owner in the store. The reasoning is narrow: the built-in number was chosen for one person's laptop, and on a shared instance the person who would exceed it is not the person who chose it. The startup error names the setting and every reason that triggered it, so you fix it once rather than one restart at a time.</p>"},{"location":"user/quickstart/#request-size","title":"Request size","text":"<p>Studio refuses a request body over 4 MiB with <code>413 request_too_large</code>, before it reads or parses the body and before it works out who is asking. That bound is about protecting the server's memory, so it deliberately sits above the limits that describe content \u2014 a workflow source, n8n or Dify, over 1 MiB is still refused as <code>source_too_large</code>, which tells you what to fix.</p> <p>Raise it with <code>KNEO_STUDIO_MAX_REQUEST_BYTES</code> (bytes) if a legitimate payload is larger. An unreadable value falls back to the default rather than preventing startup.</p>"},{"location":"user/quickstart/#login-attempts","title":"Login attempts","text":"<p>Three more bounds, on the one thing an unauthenticated caller can ask Studio to do that is genuinely expensive. The first two exist because 0.3.0's audit measured what happened without them: 32 concurrent login attempts took an unrelated request's median response time from 0.7 ms to 1.2 seconds, and 300 unauthenticated requests to the OIDC start route left 300 rows in the database that nothing reclaimed.</p> <ul> <li>Password checks run two at a time, in worker threads. A password hash is   deliberately expensive \u2014 64 MiB of memory each \u2014 so this is what stops a stream   of attempts from slowing down everyone who is already signed in. Attempts wait   briefly for a slot and are answered <code>503 login_capacity_exhausted</code> if the wait is   long. Raise with <code>KNEO_STUDIO_LOGIN_VERIFY_CONCURRENCY</code> if you have the cores and   memory for it. Under <code>oidc</code> the same slots and the same setting cover the token   verification, whose signing-key fetch waits on your identity provider; a   sign-in that finds no slot is sent back to the login page with the same code.</li> <li> <p>Ten failed attempts on one account from one address in a minute, then   <code>429 too_many_failed_attempts</code> for the rest of that minute. Counted per account   and per address, so nobody can lock a colleague out by guessing at their   name, and a correct password clears the count immediately. Change with   <code>KNEO_STUDIO_LOGIN_FAILURE_LIMIT</code>.</p> </li> <li> <p>Twenty OIDC logins started from one address in a minute, then   <code>429 too_many_login_starts</code> with <code>Retry-After: 60</code>. This one counts per   address only \u2014 not per account, because beginning a login does not name an   account yet. Change with <code>KNEO_STUDIO_LOGIN_START_LIMIT</code>. Two things worth   knowing before you tune it: if your proxy does not forward the client   address, every user counts as one client, so check   <code>KNEO_STUDIO_TRUSTED_PROXIES</code> first; and the count   is held in memory per process, so running several replicas allows the limit   each.</p> </li> </ul> <p>On <code>oidc</code>, beginning a login also stores a short-lived row. Studio keeps at most 1000 of those in flight and reclaims expired ones on every new login, answering <code>503 too_many_login_attempts</code> at the cap; <code>KNEO_STUDIO_MAX_OIDC_ATTEMPTS</code> changes the number. That cap and the start limit above are not the same bound and do not replace each other: the cap protects the database, and because it is shared, one caller filling it would refuse everyone's login \u2014 the start limit is what keeps one address from doing that. The sign-in link carries the page you were on, so you come back to it; that <code>return_to</code> path is capped at 512 bytes and ignored silently above it \u2014 no real in-app path is close.</p> <p>Every one of these falls back to its default on an unreadable value, for the same reason the request bound does. What each bound does and does not protect against \u2014 including why there is deliberately no account lockout \u2014 is in auth_design.</p>"},{"location":"user/quickstart/#import-a-workflow","title":"Import a workflow","text":"<p>Open <code>http://127.0.0.1:8091</code>. On Import, pick the Source \u2014 n8n, or Dify (see Importing a Dify app) \u2014 choose the Workflow export file, and on a bound project give the Environment alias (<code>default</code> out of the box). Then press Import. The rest of this page is the same for both sources.</p> <p>If this is your first import, use <code>examples/n8n/chat-agent.json</code>. Every n8n trigger becomes a card, because a Kneo spec has no trigger concept. For a webhook, schedule, event or chat trigger that card \u2014 Trigger must be recreated outside Kneo (<code>entry_mechanism_not_carried</code>) \u2014 is manual: it must be acknowledged before export. A manual trigger's card is <code>info</code>. For <code>chat-agent.json</code> the clean outcome is that one manual card plus one <code>info</code> note, saying the agent's tool loop runs with n8n's default iteration bound of 10, and without knowing that it is indistinguishable from something having gone wrong. Do not start from <code>hello-agent.json</code>: n8n itself refuses to run it, so it blocks with <code>source_cannot_run</code>. The examples directory also says which workflows convert, which is worth two minutes before importing something real: a Kneo spec is agent-centric, so a workflow that is mostly ordinary steps has little to map on to.</p> <p>Studio parses it, normalises it to a Kneo v1 spec, and shows you:</p> <ul> <li>the draft spec it produced (YAML is the source of truth);</li> <li>gap cards for everything it could not convert faithfully.</li> </ul> <p>Create artifact from this draft opens it in the Spec editor, which is where you get the graph, rendered from the platform's own compiled topology rather than from the YAML, so the picture and the spec cannot disagree, and the platform's verdict. Both need a bound project.</p> <p>Gaps are not warnings to skim. A <code>manual</code> gap must be acknowledged before the artifact can be exported, and a <code>blocking</code> one \u2014 an unbounded loop, say \u2014 has to be resolved.</p> <p>Clearing them is done on the Gaps page (the editor's Review gaps links there), and who may do it depends on your role in the project:</p> <ul> <li>A <code>manual</code> card needs Acknowledge this gap. The acknowledgement is recorded   against an artifact, and is for an editor or the owner.</li> <li>A <code>blocking</code> card cannot be acknowledged away. Fix it in the draft and have a   project owner Attest that the edit addressed it, or fix the source and   re-import.</li> <li>Withdraw (an attestation) and Reassociate (a renamed draft's analysis)   are owner actions too.</li> </ul> <p>A role that cannot use a control sees a sentence saying who can, not the button.</p>"},{"location":"user/quickstart/#known-limitation-how-much-of-n8ns-behaviour-studio-models","title":"Known limitation \u2014 how much of n8n's behaviour Studio models","text":"<p>To convert faithfully, Studio has to answer questions about the source: which node n8n would start from, which nodes that start actually reaches, whether two triggers run the same program, and whether an attached model is one it understands. Studio's answers are hand-written against a pinned n8n (<code>n8n@1.62.1</code>), and they are known to be incomplete.</p> <p>Nine rounds of external review found nine cases where an answer was wrong \u2014 a disconnected trigger, two webhooks entering a chain at different points, a first-party model whose node name lacks <code>Chat</code>. Every one is fixed. The limitation is not any of them; it is that the answers were encoded case by case rather than derived from n8n's own rules, so the next unusual shape may find another.</p> <p>What that means when you import:</p> <ul> <li>Ordinary workflows convert, and the gap cards are the contract. A card   says what could not be carried; no card means Studio believes it converted   faithfully.</li> <li>Studio fails toward refusing. An unrecognised node, a source with no start,   triggers that disagree, an agent the pinned n8n itself fails on every item (no   enabled model, a model its executor refuses, an <code>auto</code> prompt with no   <code>chatInput</code> to read \u2014 <code>source_cannot_run</code>, whose fix belongs in the source),   and a fan-out, one output wired to several nodes, which the source runs   in full while a Kneo graph step follows one edge (<code>branching_shape</code>, since   0.9.0) \u2014 each is a blocking card rather than a quiet conversion. For n8n   that includes a trigger wired to two steps, and one step reached by two   connections, since n8n runs a step once for every connection that reaches it.   A connection that leads back to a step is a loop, even without a loop node, and   is blocking until you pick its <code>max_steps</code>. A loop with no step in it, such as a   disabled node wired back to itself, is blocking too, because the converted workflow   cannot carry it. So is a   Switch with Send data to all matching outputs on, which can run several of   its outputs at once. You will more often be told \"I cannot convert this\" than handed   something wrong.</li> <li>Studio converts first-party n8n only, and that is a decision rather than a   backlog. <code>n8n-nodes-base</code> and <code>@n8n/n8n-nodes-langchain</code>; every other package   is refused with a card, community language models included. n8n's ecosystem is   largely community nodes, so this refuses a great deal \u2014 the alternative is   converting a node whose behaviour nobody has read, which means inventing it or   dropping it. If you need one, its card says to rebuild the behaviour as a Kneo   step; no version of Studio will convert it without a mapping you supply.</li> <li>A refused first-party node is a different problem, and the card says so. <code>n8n-nodes-base.set</code> is inside the pinned release; what Studio lacks is a Kneo   construct to convert it into. Those cards do not tell you a version is the   boundary, because it is not \u2014 changing the pin would not help.</li> <li>Three more answers were corrected in 0.5.0, each a silent conversion until   then. A node's error output (\"On Error: Continue (using error output)\")   became an unconditional edge \u2014 a pager wired to failures ran on every run; it   is now a blocking card \u2014 <code>error_route_not_carried</code> since 0.9.0, <code>error_output</code>   before \u2014 and the edge is labelled <code>error</code>.   A model node's <code>temperature</code> / <code>maxTokens</code> and an agent's <code>maxIterations</code> were   dropped without a word; they are a manual <code>parameters_not_carried</code> card   naming each value the author set and the Kneo field it belongs in. Since 0.9.0   a default n8n applies without the author writing it \u2014 the iteration bound of 10   \u2014 is an <code>info</code> note instead. And a sticky note \u2014   a canvas comment \u2014 drew a blocking card telling you to rebuild it as a step;   it is an <code>info</code> card now. Because these change what the import reports for   the same bytes, the importer and analyzer contract numbers moved \u2014 the current   ones are the <code>version</code> and <code>analyzer_version</code> on any analysis you read back, so   they are not repeated here, where they would go stale: re-import a workflow   you imported before 0.5.0 and you get a new analysis with the corrected   cards, not the old one back. 0.9.0 moved them again, and renamed seven card   kinds (<code>trigger</code> \u2192 <code>entry_mechanism_not_carried</code>, <code>no_source_start</code> \u2192   <code>no_entry</code>, and five more). A card whose id changed carries no acknowledgement   across, so acknowledge it again after re-importing.</li> <li>Always read the exported spec before you deploy it. That is good practice   regardless; here it is the backstop for a class of defect that, by   construction, this project cannot yet claim to have exhausted.</li> </ul> <p>0.3.0 derived the node maps from n8n's pinned sources \u2014 rather than extending them one case at a time \u2014 and the reasoning is recorded in its tracker in full. The start, reach and executor rules above are still read case by case, which is how 0.9.0 came to add <code>source_cannot_run</code>. (This said \"is 0.3.0's work\" for two releases after it landed \u2014 a reader lost nothing by it, which is why it outlived the cut.)</p>"},{"location":"user/quickstart/#govern-it-then-take-it-out","title":"Govern it, then take it out","text":"<p>An import produces a draft. To govern it you create an artifact, which freezes the exact bytes \u2014 base, overlays and overrides \u2014 so that what gets validated is what gets exported, and an edit afterwards cannot change either.</p> <p>From there Studio runs the platform's own <code>validate</code>, <code>compile(strict)</code> and <code>policy_report</code> against that frozen document, and renders the compiled graph.</p> <p>Export is the way out:</p> <pre><code>GET /api/projects/{project}/artifacts/{artifact}/export\n</code></pre> <p>It returns a JSON envelope \u2014 <code>{artifact_id, base_ref, spec_yaml, filename}</code>, where <code>filename</code> is a suggested save name \u2014 whose <code>spec_yaml</code> field holds the exact layered YAML. Redirecting this URL straight into a <code>.yaml</code> file gives you the envelope, not the spec; take <code>.spec_yaml</code> from it (<code>\u2026 | jq -r .spec_yaml &gt; spec.yaml</code>).</p> <p>The envelope is produced only after re-running the full gate on the precise body it is about to hand you. A stored verdict would go stale the moment an input moved, so there isn't one. It refuses, with the reason, when:</p> <ul> <li>an analysis is blocked, stale or unknown, a <code>blocking</code> gap is unresolved, or   a <code>manual</code> gap is unacknowledged (<code>409 artifact_not_ready</code>). The message is one   sentence per reason \u2014 \"1 manual gap(s) need acknowledging on the Gaps board.\"   \u2014 and the tokens (<code>artifact_unacknowledged_gaps</code>, <code>artifact_not_ready</code>,   <code>analysis_&lt;state&gt;</code>) are under <code>details.reasons</code> for code, beside <code>details.gaps</code>.   <code>chat-agent.json</code> meets this on its first export, until its trigger card is   acknowledged;</li> <li>the spec resolves something local to this environment \u2014 an operator   registry entry \u2014 so it would not mean the same thing anywhere else   (<code>artifact_not_portable</code>);</li> <li>the fresh gate returns a negative verdict (<code>artifact_governance_failed</code>), or   the platform itself rejects the spec, in which case its own status and code   are forwarded rather than flattened.</li> </ul> <p>The response is never cached: an export is an attestation about one moment.</p>"},{"location":"user/quickstart/#run-it-on-the-platform-before-you-hand-it-over","title":"Run it on the platform, before you hand it over","text":"<p>An artifact can be run, in this project's environment, exactly as export would hand it off. Open Test run \u2014 from an artifact's editor or Hand-off, the rail's link opens it for that artifact; otherwise pick one \u2014 give it an input, and start it:</p> <pre><code>POST /api/projects/{project}/test-runs      {\"artifact_id\": \"...\", \"input\": \"...\"}\nGET  /api/projects/{project}/test-runs/{run_id}\n</code></pre> <p>You are shown two statuses, and they answer different questions. Studio's says what Studio knows about the dispatch \u2014 <code>pending</code> means it has not yet confirmed the platform accepted the run; <code>started</code> means it holds exactly one platform run id; <code>failed_to_start</code> means the platform refused before anything ran; <code>unknown</code> means recovery asked and could not prove a single run; <code>abandoned</code> means recovery gave up after repeated attempts. Neither of the last two is re-issued. The platform's \u2014 queued, running, completed, failed, with the output and token usage \u2014 appears underneath, and only for a run that started. Studio never mirrors one into the other, so a queued run is never shown as \"running\".</p> <p>Refresh reads it back; nothing polls on your behalf.</p> <p>A second click is a new run, never a re-issue of the first. There is no replay in this release. If a start fails after the run row was written, the refusal names that run and the page opens it \u2014 the address carries the run (<code>\u2026/test-run/{artifact}?run={run_id}</code>), so a reload or a copied link still reads it, and its outcome is settled by recovery rather than by you pressing the button again.</p>"},{"location":"user/quickstart/#check-the-hand-off-before-you-make-it","title":"Check the hand-off before you make it","text":"<p>The Hand-off page reads the artifact's frozen binding and reports whether the environment it names can actually receive the spec \u2014 the policy the target applies, and its credential references counted by status: present, missing, or with no status Studio can read. A default platform lists its built-in providers even when none is configured, so most read missing. Values are never shown. Three answers, and the page says which it got: available; unavailable, which means the alias has no usable profile configured behind it \u2014 the zero-config first run, not an error; and a refusal, where the platform answered and its own status and code are forwarded unchanged. A platform that cannot be reached at all is neither of the first two: it is an error, <code>platform_unreachable</code>, because the absence of an answer is not the same as an answer. Doing this before the paste is what turns a failed launch into a question you can answer while you still have the artifact in front of you.</p> <p>This said an unreachable environment \"is reported as unavailable\" until   <code>[2026-09-02]</code> \u2014 the sentence collapsed a transport failure into the no-profile state, which the product, the UI and the API contract all keep apart (touchpoint 2 round 2, F-03).</p>"},{"location":"user/quickstart/#known-limitation-some-platform-valid-specs-are-refused-before-submission","title":"Known limitation \u2014 some platform-valid specs are refused before submission","text":"<p>Studio runs its own capability gate before it submits anything, and that gate classifies every field of your spec against a hand-written manifest. A field the manifest does not list is unclassified, and unclassified is refused \u2014 the gate fails closed on purpose, because a field nobody has classified might be one that runs code on a host you do not own.</p> <p>The cost of failing closed is that the manifest has drifted behind the platform's own models. Specs the platform accepts are currently refused by Studio, with <code>409 spec_capability_refused</code> on all four govern routes \u2014 <code>validate</code>, <code>compile</code>, <code>policy_report</code> and <code>graph</code> \u2014 and there is no override.</p> <p>How wide was it: every one of the 25 example specs shipped with <code>kneo_serv</code> 1.2.0 was refused \u2014 25 of 25, including the sequential and graph ones, measured 2026-08-18 by running Studio's own scanner over <code>kneo_serv/examples/*.yaml</code>.</p> <p>Re-measured 2026-08-19, after the manifest was derived from the platform's models: 19 of 25. The six that now pass are the ones that were failing purely on classification drift. Most of what remains is not drift at all:</p> <ul> <li>8 of the 19 carry a <code>components.functions.*.implementation</code> refusal \u2014 Python that   runs on the platform host. That is <code>EXECUTE</code>, and a literal value there is refused   regardless of what an operator has approved: approval attaches to a registry entry,   and the only form the scanner admits is a <code>{\"$registry\": \"&lt;name&gt;\"}</code> reference to one.   These specs get through by rewriting the field as that reference \u2014 not by approving the   literal, which nothing can approve. (This paragraph said the opposite until 0.6.0 S13;   a user following it would have asked for a standing privilege widening on a shared   platform and got the identical refusal back.)</li> <li>2 carry an <code>mcp_servers.*.url</code> refusal \u2014 <code>NETWORK</code>, refused against an empty host   allowlist, same reasoning.</li> <li>The remaining 9 are a mix, and two of their families are not in the table below.   Three overlay specs refuse on <code>policy_defaults.*</code> and one project-config spec on   <code>project.*</code>, <code>service.*</code>, <code>retention.*</code> and <code>environments.*.policy_enforcement.*</code> \u2014   neither is the table's top-level <code>policies</code> row, which is a different path. The others   are classified refusals working as intended rather than drift: <code>tools.*.implementation</code>   and <code>mcp_servers.*.command</code>/<code>.args</code> are <code>EXECUTE</code> literals, <code>skills.*.source</code> is   <code>FILESYSTEM</code>, and <code>agent.guardrails.*</code> is the free-form-map row the table does name.</li> </ul> <p>The three counts above were 12, one and \"the rest are the two groups named in the table\" until <code>[2026-09-06]</code>, and none of the three measured: the first two were wrong at every counting, and the third was false for the project-config and <code>policy_defaults</code> families. Corrected by re-running the shipped scanner over the corpus the paragraph names, and <code>test_published_capability_limits.py</code> now measures the breakdown as well as the total \u2014 the numbers above cannot rot silently again.</p> <p>The axis is the field, not the workflow family, which is what made the reach so wide: a single unclassified field anywhere refuses the whole document, and two of the most common ones sat in the <code>agent</code> block that nearly every real spec has.</p> What is in your spec Status <code>agent.strategy</code> as an object \u2014 the form the platform documents (<code>type</code>, <code>max_iterations</code>, \u2026) accepted  <code>agent.strategy</code> as a bare string accepted  <code>agent.runtime_preferences.allowed_modes</code> entries accepted  <code>workflow.participants.*</code> \u2014 the concurrent, group-chat and handoff families accepted  A human-in-the-loop component \u2014 all nine <code>components.humans</code> fields accepted  \u2026including <code>components.humans.*.context</code>, which is classified as data accepted  <code>tools.*.permissions.*</code> \u2014 <code>allow_shell</code> and the other permission flags refused <code>agent.memory</code>, <code>agent.history</code>, <code>agent.guardrails</code>, <code>agent.defaults</code>, top-level <code>policies</code>, <code>agent.model.extra</code>, <code>tools.*.mcp</code> \u2014 free-form maps the platform does not model refused <p>Verified differentially against <code>kneo_serv</code> 1.2.0: each refused item above is accepted by the platform's own models. This is pre-existing behaviour, present since 0.2.0 and not introduced by a later patch.</p> <p>This table said \"a sequential or graph workflow \u2014 accepted\" until 2026-08-18, and that was wrong in the direction that matters: a user whose sequential workflow came back <code>409 spec_capability_refused</code> had no way to read their refusal as the known limitation. It also described the humans row as \"all nine fields unclassified\", which is wrong in the other direction \u2014 <code>context</code> passes. Found by 0.3.0's Phase-1 audit (DOCS-1) by running the shipped scanner rather than by reading the manifest. Every row now carries an invisible probe marker, and <code>test_published_capability_limits.py</code> checks each one against the code, so a row that stops being true reddens a gate instead of misinforming somebody.</p> <p>What to do meanwhile. A spec whose every field the manifest classifies governs and exports normally \u2014 in practice that means hand-written specs that stay inside the narrow subset above, not exports from the platform's own examples. There is no flag that relaxes the gate, by design.</p> <p>The fix is a design change, not a longer list. A hand-maintained manifest tracking a schema it cannot see is the same failure mode that produced this drift in the first place, so the manifest is derived from the platform's own models, with a gate that reddens when the two disagree \u2014 shipped in 0.3.0. (This said \"is being derived \u2026 that work is 0.3.0\" for two releases after it landed.)</p>"},{"location":"user/quickstart/#what-this-means-for-the-editor","title":"What this means for the editor","text":"<p>Saving is not gated. Verdicts are. The editor writes any text you type: a spec is a file, and refusing to save an invalid one would lose your work mid-thought and push you into a different editor. So the gate above is met at Preview, at the three platform verdicts, and at export \u2014 never at Save. If your session expires mid-edit, Studio shows the sign-in page, and once you are signed in the unsaved edit is restored in its editor.</p> <p>That is deliberate, and it is the honest reading of the limitation: you can edit and keep any spec, and Studio will tell you it cannot judge one outside the classified subset. A <code>409 spec_capability_refused</code> from Preview is that limitation, not a fault in your file and not an outage.</p> <p>There is no override, and that is the design. A flag relaxing the gate would let an unclassified field \u2014 possibly one that runs code on a host you do not own \u2014 reach the platform unexamined, which is the whole thing failing closed prevents. What is on offer instead:</p> <ul> <li>keep editing and saving; the file is yours and Studio is not holding it;</li> <li>ask whoever runs your platform to widen the environment's allowances, if the refusal   names a capability rather than an unclassified field;</li> <li>re-import the corrected source, which gets you a machine verdict rather than a human   one.</li> </ul> <p>The editor's own refusal message says this at the point you meet it, rather than leaving you to find this page.</p>"},{"location":"user/quickstart/#next","title":"Next","text":"<ul> <li>Importing a Dify app: import_dify.md.</li> <li>Debugging a run: debugging.</li> <li>Running a shared instance: hosting.</li> </ul> <p>The reading order says which task each page covers; separate n8n-import and gap-card pages are backlog.</p>"}]}